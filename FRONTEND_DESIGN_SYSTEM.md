# YJ Knowledge Bank 前端设计系统

> 版本：1.0  
> 适用范围：仓库内首页、知识教程、专题页面、对照阅读页及后续新增的静态 HTML 页面  
> 规范入口：任何前端页面的新建、改版或样式调整，开始开发前必须先阅读本文档。

## 1. 为什么需要设计系统

知识库中的页面来自不同阶段，内容结构差异很大：有普通 Markdown 长文、带章节侧栏的深度教程、带难度页签的课程、流媒体专题，以及双栏对照阅读器。如果每个页面单独决定颜色、导航、圆角、按钮和深色模式，页面越多，视觉差异和维护成本就越大。

设计系统的职责不是让所有页面长得完全相同，而是规定它们共享的“视觉语法”：

- 相同的品牌识别、颜色、字体、间距和圆角；
- 相同的全站导航、主题切换、阅读进度和返回顶部行为；
- 相同类型的内容使用相同组件表达；
- 特殊页面可以保留自己的信息架构，但不能另起一套基础视觉；
- 新页面只需接入共享层，不再复制旧页面的导航和主题代码。

## 2. 系统架构

```text
index.html（首页，品牌视觉基准）
        │
        ├── FRONTEND_DESIGN_SYSTEM.md（规则与开发契约）
        │
        └── assets/
            ├── css/kb-design-system.css
            │   ├── Design Tokens
            │   ├── 旧变量兼容映射
            │   ├── 共享组件样式
            │   └── 响应式与无障碍规则
            │
            └── js/kb-design-system.js
                ├── 全站导航注入
                ├── 深浅主题同步
                ├── 阅读进度
                ├── 返回顶部
                └── 页面类型识别

每个 doc/**/*.html
        ├── 页面自己的内容结构与必要的专题样式
        └── 最后加载共享 CSS / JS，由共享层统一基础视觉
```

共享样式必须放在页面本地样式之后，以便兼容层覆盖旧变量：

```html
<link rel="stylesheet" href="../assets/css/kb-design-system.css">
<script defer src="../assets/js/kb-design-system.js"></script>
```

二级目录页面需要使用 `../../assets/...`。路径深度不同的页面必须先确认资源能够正常返回，再提交修改。

## 3. 设计原则

### 3.1 内容优先

视觉服务于阅读，不和内容竞争。页面首屏负责说明“这是什么、适合谁、从哪里开始”，正文负责稳定、长时间阅读。装饰元素必须克制，不能降低代码、表格或流程图的辨识度。

### 3.2 编辑感，而不是后台感

知识库采用“技术期刊 + 个人技术地图”的气质：米白纸张、深墨绿色、高对比标题、少量珊瑚色标记、等宽字体元信息。避免大面积高饱和蓝紫渐变、玻璃拟态堆叠和无意义的仪表盘组件。

### 3.3 一致，但不单调

品牌框架、排版和交互保持一致；内容表达可以根据主题变化。例如网络页面可以保留协议流程图，流媒体页面可以保留架构图，LLM 页面可以保留难度页签，但它们应共享基础颜色、表格、代码块、侧栏和导航样式。

### 3.4 渐进增强

正文在共享 JavaScript 加载失败时仍应可以阅读。导航注入、阅读进度、主题切换属于增强功能，不应成为展示正文的前置条件。

### 3.5 可访问与可持续维护

颜色不能是唯一的信息载体；按钮必须有可读名称；键盘焦点必须可见；动画需要尊重 `prefers-reduced-motion`；页面样式优先使用令牌，禁止散落新的硬编码主题色。

## 4. 颜色系统

### 4.1 核心令牌

| 语义 | CSS 变量 | 浅色 | 深色 | 使用场景 |
|---|---|---:|---:|---|
| 页面底色 | `--kb-page` | `#f4f1e9` | `#0c1715` | 页面背景 |
| 柔和表面 | `--kb-surface` | `#fbfaf6` | `#11211e` | 次级区块、工具栏 |
| 强表面 | `--kb-surface-strong` | `#ffffff` | `#162824` | 卡片、表格、正文容器 |
| 主标题 | `--kb-ink` | `#132a26` | `#edf4ef` | 标题、主按钮 |
| 正文 | `--kb-text` | `#354a45` | `#c3d0cb` | 长文本 |
| 次要文字 | `--kb-muted` | `#687771` | `#96a8a1` | 元信息、说明文字 |
| 品牌绿 | `--kb-brand` | `#0b5b4b` | `#78d5b5` | 链接、选中状态、重点 |
| 品牌浅绿 | `--kb-brand-soft` | `#dcebe4` | `#183b32` | 标签、提示框背景 |
| 珊瑚色 | `--kb-coral` | `#e9774e` | `#ff9b70` | 章节编号、进度、少量强调 |
| 蓝色 | `--kb-blue` | `#3468a8` | `#8bb8ed` | C++、工程类辅助编码 |
| 紫色 | `--kb-purple` | `#76538f` | `#c5a1dc` | AI、工具类辅助编码 |
| 边界 | `--kb-line` | `#d8ddd6` | `#29403a` | 分隔线、控件边框 |
| 代码背景 | `--kb-code` | `#10201d` | `#08110f` | 代码与终端 |

### 4.2 使用规则

1. 普通正文不得使用纯黑 `#000`；应使用 `--kb-text` 或 `--kb-ink`。
2. 链接与选中状态默认使用 `--kb-brand`。
3. 珊瑚色仅作视觉标记，单屏内不应出现超过三个大面积珊瑚色元素。
4. 新增颜色前，先判断现有语义色是否可以表达。只有数据可视化或技术图需要额外区分时才允许增加局部颜色。
5. SVG 中如果可以继承主题，优先使用 `currentColor` 或 `var(--kb-*)`；只有协议图例等固定语义色允许硬编码。
6. 文字与背景对比度应达到 WCAG AA：普通文字至少 `4.5:1`，大号文字至少 `3:1`。

## 5. 字体与排版

### 5.1 字体族

```css
--kb-font-sans: "Inter", "SF Pro Display", "Noto Sans SC",
                "PingFang SC", "Microsoft YaHei", sans-serif;

--kb-font-mono: "JetBrains Mono", "Cascadia Code",
                "SFMono-Regular", Consolas, ui-monospace, monospace;
```

- 中文正文、标题、按钮统一使用无衬线字体。
- 代码、文档编号、英文眉题、协议名元信息使用等宽字体。
- 不依赖必须联网加载的字体，避免网络失败造成页面跳动或不可读。

### 5.2 字号层级

| 层级 | 推荐字号 | 行高 | 使用说明 |
|---|---:|---:|---|
| 页面主标题 | `clamp(2.4rem, 6vw, 5.6rem)` | `1.02–1.15` | 首页或专题 Hero，仅一个 |
| 文章标题 | `clamp(2rem, 5vw, 3.2rem)` | `1.15–1.3` | 教程名称 |
| 一级章节 | `1.5–2.2rem` | `1.25–1.4` | 正文主要章节 |
| 二级章节 | `1.15–1.4rem` | `1.35–1.5` | 章节内部主题 |
| 正文 | `1rem` | `1.75–1.9` | 长文默认 |
| 辅助文字 | `0.78–0.9rem` | `1.45–1.65` | 元信息、注释 |
| 等宽元信息 | `0.68–0.78rem` | `1.4` | 编号、标签、Eyebrow |

标题使用负字距增强整体性：页面主标题约 `-0.045em`，章节标题约 `-0.025em`。正文不使用负字距。

### 5.3 阅读宽度

- 纯文本长文的正文列建议为 `760–900px`。
- 带侧栏页面整体最大宽度为 `1200px`。
- 单行中文正文尽量控制在约 30–45 个汉字。
- 表格、架构图和代码块可以突破正文文字宽度，但不得产生页面级横向滚动。

## 6. 间距、圆角与阴影

### 6.1 间距基线

使用 4px 基线，并优先选择以下序列：

```text
4  8  12  16  20  24  32  40  48  64  80  96
```

- 紧密控件内部：`8–12px`
- 卡片内边距：`20–32px`
- 卡片之间：`16–24px`
- 章节之间：`48–80px`
- 页面首尾留白：桌面端 `64–100px`，移动端 `40–64px`

禁止为了解决局部错位随意加入 `13px`、`27px`、`43px` 等无体系数值，除非是像素对齐需要并附有说明。

### 6.2 圆角

| 令牌 | 数值 | 使用场景 |
|---|---:|---|
| `--kb-radius-sm` | `8px` | 小控件、内联元素 |
| `--kb-radius-md` | `12px` | 普通卡片、代码块、表格 |
| `--kb-radius-lg` | `18px` | 大区块、章节容器 |
| `--kb-radius-xl` | `24px` | Hero 浮层、重点容器 |
| 胶囊 | `999px` | 筛选器、状态标签 |

同一个视觉层级中不要混用三种以上圆角。

### 6.3 阴影

知识页面以边框分层为主，阴影只用于“浮起”的对象：顶部工具、悬停卡片、返回顶部按钮。静态正文区块不要大面积使用深阴影。

## 7. 页面布局类型

### 7.1 普通长文页

适用于 Markdown 渲染教程和单列文章。

```text
┌────────────────── 全站导航 ──────────────────┐
│                                              │
│             文章标题 / 摘要 / 标签            │
│                                              │
├──────────────────────────────────────────────┤
│             760–900px 正文列                  │
│             标题 / 段落 / 代码                │
└──────────────────────────────────────────────┘
```

### 7.2 课程页

适用于带入门、中等、进阶页签的内容。全站导航在第一层，课程页签作为第二层粘性工具栏，二者职责不能混合。

### 7.3 侧栏教程页

```text
┌────────────────── 全站导航 ──────────────────┐
├────────────── 专题 Hero ─────────────────────┤
│  220px 侧栏  │  主内容区 minmax(0, 1fr)       │
│  sticky TOC  │  chapter / panel / diagram     │
│              │                                │
└──────────────┴────────────────────────────────┘
```

- 侧栏只放当前文章目录，不放全站文章链接。
- 小于页面既有移动端断点时，侧栏应折叠、隐藏或改为横向目录。
- 主内容必须设置 `min-width: 0`，避免代码和表格撑破网格。

### 7.4 双栏对照页

双栏页是全高应用型布局，不注入第二套全站导航。它保留自己的紧凑导航和工具栏，共享主题令牌、字体与控件规范。两个 iframe 中的文章使用 `kb-embedded` 阅读样式。

## 8. 核心组件规范

### 8.1 全站导航 `kb-sitebar`

由共享脚本自动注入，页面不得再次手写同功能的品牌导航。

| 区域 | 内容 |
|---|---|
| 左侧 | 品牌图标 + `YJ Knowledge Bank`，点击返回首页 |
| 中间 | 当前页面标题，空间不足时隐藏 |
| 右侧 | 深浅主题、GitHub |
| 底部 | 阅读进度线 |

页面可以在其下保留“当前教程页签”或“章节快捷导航”，但不能重复显示品牌、首页、GitHub 和主题按钮。

### 8.2 Hero

- 必须包含一个 `h1`。
- 摘要用一到两句话说明读者收益，避免堆砌关键词。
- 标签用于内容分类，不作为装饰。
- Hero 可以有主题图解，但移动端必须自然换行。
- 同一页面不允许出现两个视觉权重相同的 Hero。

### 8.3 卡片

- 卡片用于可点击入口、概念分组或独立的信息单元。
- 整卡可点击时不要在内部再嵌套其他链接。
- 标题、摘要、元信息顺序固定。
- 悬停最多上移 `3–6px`，不能做大幅缩放。
- 纯正文不应为了“好看”全部包进卡片。

### 8.4 侧栏目录

- 当前章节使用品牌色、浅绿背景和左边界表示。
- 链接文本优先简短，建议不超过 18 个汉字。
- 页面滚动时目录可以粘性定位，但不得遮挡全站导航。

### 8.5 代码与终端

- 块级代码统一深色背景，即使页面处于浅色主题。
- 使用 `--kb-font-mono`。
- 保留可复制的真实文本，不把代码转为图片。
- 长行优先允许代码块内部横向滚动，不能撑破整个页面。
- 内联代码使用浅绿背景和品牌色，不使用高饱和红色。

### 8.6 表格

- 表头使用 `--kb-brand-soft`，正文使用强表面色。
- 表格外层需要在移动端支持局部横向滚动。
- 不依靠颜色单独表达状态，需同时提供文字或符号。
- 超过七列时，优先考虑拆表、卡片列表或对比矩阵。

### 8.7 提示框

| 类型 | 边界颜色 | 适用内容 |
|---|---|---|
| 普通知识点 | 品牌绿 | 定义、关键概念、最佳实践 |
| 警告 | 警告色 | 常见错误、风险、兼容性问题 |
| 类比 | 品牌绿或蓝色 | 帮助建立直觉的解释 |
| 实验 | 品牌绿 | 可执行步骤和验证方法 |

提示框标题应说明信息类型，例如“为什么”“常见误区”“动手实验”，不要只写“注意”。

## 9. 响应式标准

共享系统使用以下主要断点：

| 断点 | 含义 | 预期变化 |
|---|---|---|
| `> 1200px` | 宽桌面 | 内容保持最大宽度，不无限拉伸 |
| `901–1200px` | 普通桌面/平板横屏 | 缩小网格间距，保留侧栏 |
| `721–900px` | 平板 | 隐藏部分导航信息，侧栏按页面规则折叠 |
| `≤ 720px` | 手机 | 单列布局、54px 导航、缩小页面边距 |
| `≤ 430px` | 小屏手机 | 隐藏次要工具、按钮允许占满宽度 |

每次新增或修改页面，至少验证：

```text
桌面：1280 × 720
手机：390 × 844
极窄：320 × 568（结构检查即可）
```

验收条件：

- `document.documentElement.scrollWidth <= document.documentElement.clientWidth`
- 导航不会遮挡锚点标题
- 字号无需用户缩放即可阅读
- 表格和代码的溢出发生在组件内部，而不是页面级
- 点击区域至少约 `40 × 40px`

## 10. 主题系统

主题状态统一保存到：

```js
localStorage.setItem('theme', 'light' | 'dark')
```

页面通过根元素属性响应：

```html
<html data-theme="dark">
```

开发规则：

1. 新组件必须同时验证浅色和深色。
2. 不要在新页面中再实现独立主题切换器。
3. 不要使用 `filter: invert()` 粗暴转换图片或图表。
4. SVG 优先引用 CSS 变量；Mermaid 等第三方渲染需要按主题重新初始化时，可以保留局部同步逻辑。
5. iframe 页面通过 `postMessage({ type: 'setTheme', value })` 同步主题。

## 11. 交互与无障碍

### 11.1 必须满足

- 所有图片具有准确 `alt`；纯装饰图片使用 `alt=""`。
- 图标按钮必须有 `aria-label` 和 `title`。
- 页面中只能有一个主 `h1`，标题层级不能跳跃。
- 可点击元素使用 `a` 或 `button`，不要给普通 `div` 绑定点击模拟按钮。
- 键盘焦点不能被 `outline: none` 移除。
- 锚点跳转需要考虑粘性导航高度。
- 自动动画必须支持 `prefers-reduced-motion`。
- 外部链接使用 `target="_blank"` 时必须配合 `rel="noopener"`。

### 11.2 动效边界

- 基础过渡时间：`160–220ms`。
- 列表筛选和主题切换不能产生强烈闪烁。
- 不使用无限循环装饰动画，协议流动演示除外。
- 悬停反馈优先使用颜色、边框和轻微位移，不使用大幅旋转或缩放。

## 12. 新页面开发流程

### 步骤 1：先确定页面类型

在普通长文、课程页、侧栏教程、双栏对照页中选择最接近的一种。不要从空白页面重新发明布局。

### 步骤 2：使用语义化骨架

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="准确描述页面内容">
  <title>页面标题 — YJ Knowledge Bank</title>
  <link rel="icon" href="../img/icons/icon1.png">

  <!-- 页面真正需要的专题样式 -->
  <style>...</style>

  <!-- 必须最后加载共享设计层 -->
  <link rel="stylesheet" href="../assets/css/kb-design-system.css">
  <script defer src="../assets/js/kb-design-system.js"></script>
</head>
<body>
  <header class="hero">...</header>
  <main>...</main>
</body>
</html>
```

### 步骤 3：优先复用共享变量

推荐：

```css
.protocol-card {
  color: var(--kb-text);
  border: 1px solid var(--kb-line);
  border-radius: var(--kb-radius-md);
  background: var(--kb-surface-strong);
}
```

禁止：

```css
.protocol-card {
  color: #222;
  border: 1px solid #ddd;
  border-radius: 13px;
  background: #fff;
}
```

### 步骤 4：只写专题差异

页面本地 CSS 只负责共享系统没有表达的内容，例如协议时序图、媒体管线、特定交互控件。导航、通用卡片、代码块、表格、主题按钮不应重复实现。

### 步骤 5：完成验收

```text
[ ] 正确接入共享 CSS / JS，Network 面板无 404
[ ] 页面只有一个全站品牌导航
[ ] 1280px 与 390px 无页面级横向溢出
[ ] 浅色与深色主题均清晰可读
[ ] 目录、页签、搜索、测验等原有交互正常
[ ] 控制台无 error / warning
[ ] 所有站内链接仍然有效
[ ] 键盘可以访问主要控件
[ ] prefers-reduced-motion 下无强制动画
```

## 13. 修改共享系统的规则

共享 CSS/JS 会影响所有知识页面，修改时必须比修改单页更谨慎。

1. 优先增加语义令牌或精确页面类型规则，不要添加范围过大的 `!important`。
2. 旧页面变量映射集中维护在共享 CSS 顶部，不要在多个页面重复补丁。
3. 修改后至少抽检以下代表页面：
   - 首页：`index.html`
   - 普通长文：`doc/frp-nginx-networking-guide.html`
   - 课程页：`doc/llm-tutorial.html`
   - 侧栏教程：`doc/FPlayer-Desktop-技术全景教程.html`
   - 网络专题：`doc/network/网络原来如此-基础教程.html`
   - 双栏页：`doc/thread/cpp-threading-comparison.html`
4. 如果共享规则只为修复一个页面，先确认是否应该使用该页面的限定类，而不是污染所有页面。
5. 不删除旧页面的重要专题样式，除非已经确认共享层完整覆盖并完成视觉回归。

## 14. 文件职责

| 文件 | 职责 | 修改频率 |
|---|---|---|
| `index.html` | 首页品牌基准与知识入口 | 内容或首页结构变化时 |
| `assets/css/kb-design-system.css` | 全局令牌、组件与兼容层 | 谨慎、需全站回归 |
| `assets/js/kb-design-system.js` | 全站外壳、主题和阅读交互 | 谨慎、需交互回归 |
| `doc/**/*.html` | 专题内容与必要的局部表现 | 按内容更新 |
| `FRONTEND_DESIGN_SYSTEM.md` | 设计规则与开发契约 | 规则变化时同步更新 |
| `AGENTS.md` | 强制开发入口 | 仅规则入口变化时 |

## 15. 关键概念速查表

| 概念 | 结论 |
|---|---|
| 开发前置 | 修改任何前端页面前先阅读本文档 |
| 品牌气质 | 技术期刊 + 个人技术地图，克制、清晰、有编辑感 |
| 主色 | 深墨绿 `--kb-brand`，珊瑚色只作少量强调 |
| 正文字体 | 系统无衬线字体栈 |
| 代码字体 | `--kb-font-mono` |
| 页面最大宽度 | 普通长文 `900px`，带侧栏 `1200px` |
| 间距 | 4px 基线，优先使用标准序列 |
| 卡片圆角 | 普通 `12px`，大区块 `18px` |
| 全站导航 | 由 `kb-design-system.js` 自动注入，不手写重复导航 |
| 深色模式 | 根节点 `data-theme` + `localStorage.theme` |
| 响应式验收 | 至少检查 `1280×720` 与 `390×844` |
| 局部样式 | 只实现专题差异，颜色与基础组件必须复用令牌 |
| 全站修改 | 共享 CSS/JS 变更必须抽检六类代表页面 |

