---
tags:
  - c++
  - qt
  - design-patterns
  - software-engineering
created: 2026-07-01
aliases:
  - GoF Design Patterns
  - C++ 设计模式
  - Qt 设计模式
---

# 设计模式完全指南（C++ / Qt）

## 概述

设计模式（Design Patterns）是软件工程中针对反复出现的问题的通用、可复用解决方案。1994 年 GoF（Gang of Four）在《Design Patterns: Elements of Reusable Object-Oriented Software》中系统归纳了 23 种经典模式，分为三大类：**创建型**、**结构型**、**行为型**。

本文以 C++ 为实现语言，重点结合 **Qt 框架**中的实际应用——Qt 本身就是设计模式的最佳教学案例，其源码中大量运用了经典模式。

> [!note] 学习建议
> 不必一次性背下所有模式。先理解每个模式的**意图**（它解决什么问题），然后在你自己的代码中"遇到类似痛点时"再回头查阅具体实现。模式是工具箱，不是教条。

## 1. 设计原则：SOLID

在学模式之前，先理解 SOLID 五大设计原则——设计模式本质上是对这些原则的具体化应用。

```mermaid
flowchart TD
    subgraph SOLID["SOLID 五大设计原则"]
        S["<b>S</b> 单一职责原则 (SRP)<br>一个类只应该有一个引起它变化的原因<br>→ Decorator、Observer"]
        O["<b>O</b> 开闭原则 (OCP)<br>对扩展开放，对修改关闭<br>→ Strategy、Decorator、Factory Method"]
        L["<b>L</b> 里氏替换原则 (LSP)<br>子类对象应能替换父类对象<br>→ Template Method、所有继承模式"]
        I["<b>I</b> 接口隔离原则 (ISP)<br>不应强迫客户端依赖不用的方法<br>→ Adapter"]
        D["<b>D</b> 依赖倒置原则 (DIP)<br>高层模块不依赖低层模块，两者依赖抽象<br>→ Factory Method、Abstract Factory、Bridge"]
    end
    style S fill:#dbeafe,stroke:#2563eb
    style O fill:#ede9fe,stroke:#7c3aed
    style L fill:#fff7ed,stroke:#ea580c
    style I fill:#ecfdf5,stroke:#059669
    style D fill:#fef3c7,stroke:#d97706
```

### 1.1 C++ 中的 SOLID 示例

```cpp
// 违反 DIP：高层直接依赖低层
class MySQLDatabase {
public:
    void insert(const QString& data) { /* ... */ }
};

class UserService {
    MySQLDatabase db;  // ❌ 依赖具体类，无法切换到 PostgreSQL
public:
    void saveUser(const QString& user) { db.insert(user); }
};

// 遵循 DIP：依赖抽象接口
class IDatabase {
public:
    virtual ~IDatabase() = default;
    virtual void insert(const QString& data) = 0;
};

class MySQLDatabase : public IDatabase { /* ... */ };
class PostgresDatabase : public IDatabase { /* ... */ };

class UserService {
    IDatabase& db;  // ✅ 依赖抽象，可注入任意实现
public:
    UserService(IDatabase& db) : db(db) {}
    void saveUser(const QString& user) { db.insert(user); }
};
```

## 2. 模式分类总览

```mermaid
flowchart TD
    GOF["<b>GoF 设计模式 (23 种)</b>"] --> CREATIONAL["<b>创建型 (5 种)</b>"]
    GOF --> STRUCTURAL["<b>结构型 (7 种)</b>"]
    GOF --> BEHAVIORAL["<b>行为型 (11 种)</b>"]

    CREATIONAL --> C1["Singleton · Factory Method<br>Abstract Factory · Builder · Prototype"]
    STRUCTURAL --> S1["Adapter · Bridge · Composite · Decorator<br>Facade · Flyweight · Proxy"]
    BEHAVIORAL --> B1["Chain of Resp. · Command · Iterator<br>Mediator · Memento · Observer<br>State · Strategy · Template Method<br>Visitor · Interpreter"]

    style GOF fill:#1e293b,color:#fff
    style CREATIONAL fill:#dbeafe,stroke:#2563eb
    style STRUCTURAL fill:#ede9fe,stroke:#7c3aed
    style BEHAVIORAL fill:#ecfdf5,stroke:#059669
```

## 3. 创建型模式（Creational Patterns）

创建型模式抽象了对象的实例化过程，使系统独立于其对象的创建、组合和表示方式。

### 3.1 Singleton（单例）

> **意图**：确保一个类只有一个实例，并提供全局访问点。

#### 生产场景：日志系统崩溃记

凌晨两点，线上服务突然挂了。你登录服务器查看日志，发现日志文件被截成了三段——因为三个模块各自 `new` 了自己的 `FileLogger`，它们同时往同一个文件写，互相覆盖，最终谁也写不进去。

```cpp
// 模块 A
FileLogger loggerA("/var/log/app.log");  // 打开文件句柄 A
// 模块 B
FileLogger loggerB("/var/log/app.log");  // 打开文件句柄 B ← 两个句柄抢同一个文件！
```

更糟的是，你想统一加一个"日志级别过滤"功能，结果发现要改 12 个分散在各处的 `Logger` 构造。**核心问题：某个对象在系统中只应该有"一个"，但没有任何机制约束这一点。**

这就是 Singleton 要解决的问题——不是"大家都用一个对象"的建议，而是**强制**保证只有一个实例。

```mermaid
flowchart LR
    A["模块 A"] -->|"new Config()"| C1["Config v1<br>debug=true"]
    B["模块 B"] -->|"new Config()"| C2["Config v2<br>debug=false"]
    C1 --> PROBLEM["两份配置<br>状态可能不同步"]
    C2 --> PROBLEM
    style PROBLEM fill:#fee2e2,stroke:#dc2626
```

#### 结构

```mermaid
classDiagram
    class Singleton {
        -static instance : Singleton
        -Singleton()
        +static getInstance() Singleton&
        +operation()
    }
```

#### C++ 实现（线程安全 Meyers Singleton）

```cpp
class ConfigManager {
public:
    // C++11 保证静态局部变量初始化的线程安全
    static ConfigManager& getInstance() {
        static ConfigManager instance;
        return instance;
    }

    ConfigManager(const ConfigManager&) = delete;
    ConfigManager& operator=(const ConfigManager&) = delete;

    void setDebugMode(bool enabled) { m_debug = enabled; }
    bool isDebugMode() const { return m_debug; }

private:
    ConfigManager() = default;  // 构造函数私有
    bool m_debug = false;
};

// 使用
ConfigManager::getInstance().setDebugMode(true);
```

#### Qt 中的单例

```cpp
// QApplication 本质上是单例（每个进程只有一个）
QApplication app(argc, argv);

// QSettings 通常作为单例使用
class AppSettings {
public:
    static AppSettings& instance() {
        static AppSettings s;
        return s;
    }

    QVariant value(const QString& key) const {
        return m_settings.value(key);
    }

private:
    AppSettings() : m_settings("MyCompany", "MyApp") {}
    QSettings m_settings;
};
```

> [!warning] 慎用单例
> 单例本质上是**全局状态**，会让单元测试变得困难（测试之间状态互相污染）。Qt 项目中，优先考虑用依赖注入代替单例。仅在真正需要"有且仅有一个实例"的场景使用（如日志系统、硬件设备句柄）。

### 3.2 Factory Method（工厂方法）

> **意图**：定义一个用于创建对象的接口，让子类决定实例化哪个类。

#### 生产场景：支付渠道的"if-else 地狱"

你接到了一个电商平台的支付模块。一开始只接了支付宝，代码很简洁：

```cpp
void pay(const Order& order) {
    AlipayService alipay;
    alipay.pay(order.amount, order.id);
}
```

半年后，产品说"我们要接微信支付"。你加了个 `if (order.channel == "wechat")`。又过了一个月，银联来了，又是 `else if`。等到要接 Apple Pay 和 PayPal 时，`pay()` 方法已经 200 行 `if-else`，新增一个渠道要改 4 个文件，测试说"每次你改支付，我就得把所有渠道全回归一遍"。

**核心问题：打开文档/处理支付的**流程**是固定的（创建服务 → 发起支付 → 记录日志），但"创建哪个具体的支付服务"要根据运行时条件决定。** 你希望新增一个渠道时，只需加一个新类，不动已有代码。

#### 结构

```mermaid
classDiagram
    class Creator {
        +someOperation()
        +createProduct()* Product
    }
    class ConcreteCreator {
        +createProduct() Product
    }
    class Product {
        +operation()
    }
    class ConcreteProductA {
        +operation()
    }
    class ConcreteProductB {
        +operation()
    }

    Creator <|-- ConcreteCreator
    Product <|-- ConcreteProductA
    Product <|-- ConcreteProductB
    Creator ..> Product : creates
    ConcreteCreator ..> ConcreteProductA : creates
```

#### C++ 实现

```cpp
// 产品接口
class Parser {
public:
    virtual ~Parser() = default;
    virtual Document parse(const QString& filePath) = 0;
};

class JsonParser : public Parser {
public:
    Document parse(const QString& filePath) override {
        qDebug() << "Parsing JSON:" << filePath;
        return Document{};
    }
};

class XmlParser : public Parser {
public:
    Document parse(const QString& filePath) override {
        qDebug() << "Parsing XML:" << filePath;
        return Document{};
    }
};

// 创建者（工厂方法由子类实现）
class DocumentEditor {
public:
    virtual ~DocumentEditor() = default;

    // 模板方法：定义算法骨架，将创建步骤延迟到子类
    void openDocument(const QString& path) {
        auto parser = createParser(path);  // 工厂方法
        auto doc = parser->parse(path);
        m_currentDoc = doc;
        render();
    }

protected:
    virtual std::unique_ptr<Parser> createParser(const QString& path) = 0;

private:
    void render() { /* 通用渲染逻辑 */ }
    Document m_currentDoc;
};

class JsonEditor : public DocumentEditor {
protected:
    std::unique_ptr<Parser> createParser(const QString&) override {
        return std::make_unique<JsonParser>();
    }
};
```

#### Qt 中的工厂方法

```cpp
// QMimeDatabase 根据文件内容决定创建哪种类型的对象
QMimeDatabase db;
QMimeType mime = db.mimeTypeForFile("data.json");
// mime.name() == "application/json" → 内部根据 MIME 类型"创建"不同的处理器

// 实际模式：Qt 插件系统
// 加载插件时，每个插件提供一个"工厂方法"来创建具体实例
QPluginLoader loader("plugins/myformat.dll");
auto* factory = qobject_cast<FormatFactoryInterface*>(loader.instance());
auto* parser = factory->createParser();  // 工厂方法
```

### 3.3 Abstract Factory（抽象工厂）

> **意图**：提供一个接口来创建**一系列相关或相互依赖的对象**，而无需指定它们的具体类。

#### 生产场景：跨平台 UI 的"混搭灾难"

你开发了一个桌面应用，第一版只跑 Windows，一切正常。后来老板说"出个 Mac 版"。你信心满满地把代码拉过去编译，结果傻眼了：

```
Mac 版界面上，按钮是 Windows 9x 的直角风格，
下拉框却是 macOS 的圆角 Aqua 风格，
最离谱的是滚动条——一半 Windows 灰一半 Mac 蓝。
```

原因很简单：你在各处散落着 `new WindowsButton()`、`new MacComboBox()`。改了一个控件忘记改另一个，就变成了"缝合怪"。**核心问题：需要创建的不是一个对象，而是**一整套彼此配套的对象**（按钮、输入框、下拉菜单必须风格统一）。你不能让调用方自己组合——必须有一个"工厂"一次性创建整族产品。

```mermaid
flowchart TD
    WF["<b>WidgetFactory</b><br>+createButton()<br>+createLineEdit()<br>+createComboBox()"]
    WF --> Win["<b>WindowsFactory</b><br>创建 Windows 风格控件族"]
    WF --> Mac["<b>MacFactory</b><br>创建 macOS 风格控件族"]
    WF --> Linux["<b>LinuxFactory</b><br>创建 Linux 风格控件族"]
    Win --> WinP["WindowsButton<br>WindowsLineEdit<br>WindowsComboBox"]
    Mac --> MacP["MacButton<br>MacLineEdit<br>MacComboBox"]
    Linux --> LinuxP["LinuxButton<br>LinuxLineEdit<br>LinuxComboBox"]

    style WF fill:#1e293b,color:#fff
    style Win fill:#dbeafe,stroke:#2563eb
    style Mac fill:#ede9fe,stroke:#7c3aed
    style Linux fill:#ecfdf5,stroke:#059669
```

#### C++ 实现

```cpp
// 抽象产品
class Button {
public:
    virtual ~Button() = default;
    virtual void render() = 0;
};

class LineEdit {
public:
    virtual ~LineEdit() = default;
    virtual void render() = 0;
};

// 具体产品族 — Windows
class WindowsButton : public Button {
public:
    void render() override { /* GDI 绘制 */ }
};
class WindowsLineEdit : public LineEdit {
public:
    void render() override { /* GDI 绘制 */ }
};

// 具体产品族 — macOS
class MacButton : public Button {
public:
    void render() override { /* Metal 绘制 */ }
};
class MacLineEdit : public LineEdit {
public:
    void render() override { /* Metal 绘制 */ }
};

// 抽象工厂
class WidgetFactory {
public:
    virtual ~WidgetFactory() = default;
    virtual std::unique_ptr<Button> createButton() = 0;
    virtual std::unique_ptr<LineEdit> createLineEdit() = 0;
};

// 具体工厂
class WindowsWidgetFactory : public WidgetFactory {
public:
    std::unique_ptr<Button> createButton() override {
        return std::make_unique<WindowsButton>();
    }
    std::unique_ptr<LineEdit> createLineEdit() override {
        return std::make_unique<WindowsLineEdit>();
    }
};

// 客户端：与具体平台解耦
class Application {
    std::unique_ptr<WidgetFactory> m_factory;
public:
    Application(std::unique_ptr<WidgetFactory> factory)
        : m_factory(std::move(factory)) {}

    void buildUI() {
        auto btn = m_factory->createButton();
        auto edit = m_factory->createLineEdit();
        btn->render();
        edit->render();
        // ✅ 保证 btn 和 edit 属于同一个产品族
    }
};
```

#### Qt 中的抽象工厂

Qt 的风格系统本质上是抽象工厂：

```cpp
// QStyle 就是抽象工厂
// QWindowsStyle、QMacStyle、QFusionStyle 是具体工厂

QApplication::setStyle("Fusion");  // 切换整个控件族的风格

// QStyle 内部创建每个控件的绘制器
// style->drawControl(QStyle::CE_PushButton, ...)
// style->drawPrimitive(QStyle::PE_FrameLineEdit, ...)
```

### 3.4 Builder（建造者）

> **意图**：将复杂对象的**构建过程**与**表示**分离，使得同样的构建过程可以创建不同的表示。

#### 生产场景："参数位置传错了，为什么没报错？"

你负责的邮件服务出了一个诡异的 bug：用户收到了一封"无主题"邮件，正文出现在抄送栏里。查了半天发现——有人调用构造函数时把 `subject` 和 `body` 的参数位置搞反了：

```cpp
// 构造函数：Mail(subject, body, cc, bcc, attachments, priority, charset...)
Mail mail("", "请审批这份合同", "boss@corp.com", "", {}, 1, "UTF-8");
//          ↑ subject=""  ↑ body="请审批..."                 ↑ cc="boss@..."
//  实际效果：subject 是空字符串，body 被当成了 subject，cc 被填入了 body...
```

当构造参数超过 5 个，且大部分是相同类型（全是 `QString`），**编译器不会报错**——它只看类型是否匹配，不管语义。更糟的是，不同场景需要不同的参数组合：内部邮件不需要附件和优先级，对外邮件却需要全部字段。

**核心问题：如何安全地构造一个参数多、可选字段多、还有约束关系的复杂对象？** 需要的不是一个超大构造函数，而是一种"分步填写，最后验证，一次性构建"的方式。

#### 结构

```mermaid
classDiagram
    class Director {
        +construct()
    }
    class Builder {
        +buildPartA()
        +buildPartB()
        +getResult() Product
    }
    class ConcreteBuilder {
        +buildPartA()
        +buildPartB()
        +getResult() Product
    }
    class Product {
    }

    Director --> Builder : uses
    Builder <|-- ConcreteBuilder
    ConcreteBuilder ..> Product : creates
```

#### C++ 实现（流式 Builder）

```cpp
class HttpRequest {
public:
    QString method;
    QString url;
    QMap<QString, QString> headers;
    QByteArray body;
    int timeoutMs = 5000;
    int retryCount = 0;

    // Builder 作为嵌套类
    class Builder {
    public:
        Builder& setMethod(const QString& method) {
            m_request.method = method;
            return *this;
        }
        Builder& setUrl(const QString& url) {
            m_request.url = url;
            return *this;
        }
        Builder& addHeader(const QString& key, const QString& val) {
            m_request.headers[key] = val;
            return *this;
        }
        Builder& setTimeout(int ms) {
            m_request.timeoutMs = ms;
            return *this;
        }
        Builder& setRetryCount(int n) {
            m_request.retryCount = n;
            return *this;
        }
        HttpRequest build() {
            // 在这里做验证
            Q_ASSERT(!m_request.url.isEmpty());
            return std::move(m_request);
        }
    private:
        HttpRequest m_request;
    };

private:
    HttpRequest() = default;  // 强制通过 Builder 创建
};

// 使用 — 流式调用，清晰易读
auto request = HttpRequest::Builder()
    .setMethod("POST")
    .setUrl("https://api.example.com/data")
    .addHeader("Authorization", "Bearer xxx")
    .addHeader("Content-Type", "application/json")
    .setTimeout(3000)
    .setRetryCount(2)
    .build();
```

#### Qt 中的 Builder

```cpp
// QProcess 的参数设置就是 Builder 模式
QProcess process;
process.setProgram("ffmpeg");
process.setArguments({"-i", "input.mp4", "-codec", "copy", "output.mkv"});
process.setWorkingDirectory("/tmp");
process.setProcessChannelMode(QProcess::MergedChannels);
process.start();  // build() 的等价物

// 更典型的场景：QNetworkRequest 的构造
QNetworkRequest request(QUrl("https://api.example.com"));
request.setHeader(QNetworkRequest::ContentTypeHeader, "application/json");
request.setRawHeader("Authorization", "Bearer xxx");
request.setTransferTimeout(5000);
// → 逐步"建造"一个复杂的请求对象
```

### 3.5 Prototype（原型）

> **意图**：通过**拷贝**已有对象来创建新对象，而不是通过 new。

#### 生产场景：文档模板的"格式传染"

你做了一个在线文档工具，用户可以设置字体、颜色、页边距来创建"规范模板"。然后其他同事基于这个模板创建新文档——他们需要的是"和这个模板一模一样的格式"，但内容要从空白开始。

如果你让用户每次都手动重新设置几十个格式属性，没人会用。另一种方案是给模板加个 `int useCount` 引用计数——但用户改了自己副本的颜色后，模板的原始格式不能受影响。

**核心问题：需要基于一个"原型对象"创建新对象，新对象和原型初始状态一样，但后续修改互不影响。** 最自然的方案就是"深拷贝"。

#### C++ 实现

```cpp
// 抽象原型
class Shape {
public:
    virtual ~Shape() = default;
    virtual std::unique_ptr<Shape> clone() const = 0;
    virtual void draw(QPainter& painter) = 0;

    // 属性
    QColor strokeColor = Qt::black;
    Qt::PenStyle strokeStyle = Qt::SolidLine;
    int cornerRadius = 0;
    QColor shadowColor = Qt::transparent;
};

class Rectangle : public Shape {
public:
    int width = 100;
    int height = 60;

    std::unique_ptr<Shape> clone() const override {
        return std::make_unique<Rectangle>(*this);  // 利用拷贝构造
    }

    void draw(QPainter& painter) override {
        painter.setPen(QPen(strokeColor, 1, strokeStyle));
        painter.drawRoundedRect(0, 0, width, height, cornerRadius, cornerRadius);
    }
};

// 使用
auto original = std::make_unique<Rectangle>();
original->strokeColor = Qt::red;
original->strokeStyle = Qt::DashLine;
original->cornerRadius = 8;
original->shadowColor = QColor(0, 0, 0, 50);

auto clone = original->clone();  // ✅ 获得一个相同配置的新对象
```

#### Qt 中的原型

```cpp
// QObject 没有 clone()（因为有 QObject 的拷贝语义限制）
// 但数据类通常支持拷贝：

QFont original("Arial", 14);
original.setBold(true);
original.setItalic(true);

QFont clone = original;  // QFont 有完善的拷贝构造 → 原型模式

// QGradient 也是
QLinearGradient grad(0, 0, 100, 100);
grad.setColorAt(0, Qt::red);
grad.setColorAt(1, Qt::blue);
// 无法直接 clone()，但拷贝构造同样实现了原型模式的意图
```

**创建型模式选择指南：**

```mermaid
flowchart TD
    Q["<b>变化的是什么？</b>"] --> Q1{"只需要一个实例？"}
    Q1 -->|是| A1["<b>Singleton</b>"]
    Q1 -->|否| Q2{"需要创建什么类型<br>的对象（单个）？"}
    Q2 -->|创建逻辑在子类中| A2["<b>Factory Method</b>"]
    Q2 -->|否| Q3{"需要创建一系列<br>相关对象？"}
    Q3 -->|产品族之间有约束| A3["<b>Abstract Factory</b>"]
    Q3 -->|否| Q4{"对象构造参数<br>太多太复杂？"}
    Q4 -->|分步构建| A4["<b>Builder</b>"]
    Q4 -->|否| Q5{"需要基于现有<br>对象创建？"}
    Q5 -->|运行时克隆| A5["<b>Prototype</b>"]

    style A1 fill:#dbeafe,stroke:#2563eb
    style A2 fill:#ede9fe,stroke:#7c3aed
    style A3 fill:#fff7ed,stroke:#ea580c
    style A4 fill:#ecfdf5,stroke:#059669
    style A5 fill:#fef3c7,stroke:#d97706
```

---

## 4. 结构型模式（Structural Patterns）

结构型模式关注类和对象的组合方式，通过继承或组合来形成更大的结构。

### 4.1 Adapter（适配器）

> **意图**：将一个类的接口转换成客户端期望的另一个接口，使原本不兼容的类能一起工作。

#### 生产场景：老 C 库的"水土不服"

公司买了一个人脸识别 SDK，性能极好，但接口是这样的：

```cpp
// 老式 C 风格：返回 int 表示错误码，结果通过指针参数传出
int face_detect(const char* img_path, int img_len, FACE_RECT** results, int* count);
```

而你的项目是 Qt 技术栈，期望的接口风格是：

```cpp
QList<QRect> detectFaces(const QImage& image);  // 抛异常或返回空列表
```

你不能改 SDK（没源码），也不能让 Qt 代码去适配 C 风格（那会让整个项目充满 `char*` 和裸指针）。更麻烦的是，这个 SDK 只是过渡方案——半年后可能换成自研引擎。

**核心问题：如何让一个"接口不兼容"的类无缝融入现有体系，并且未来替换它时不影响调用方？** 答案是在中间加一层"翻译"——适配器。

```mermaid
classDiagram
    class Client {
    }
    class Target {
        +request()
    }
    class Adapter {
        +request()
    }
    class Adaptee {
        +specificRequest()
    }

    Client --> Target : depends on
    Target <|-- Adapter
    Adapter --> Adaptee : wraps
    note for Adapter "request() 内部调用 specificRequest()"
```

#### C++ 实现（对象适配器）

```cpp
// Adaptee：第三方 JSON 库的接口（假设的旧式 C 风格）
class LegacyJsonLib {
public:
    bool parse(const std::string& text) {
        // 复杂的旧式解析逻辑...
        m_parsed = !text.empty();
        return m_parsed;
    }

    std::string getValue(const std::string& key) const {
        return m_data.value(key, "");
    }

private:
    bool m_parsed = false;
    std::map<std::string, std::string> m_data;
};

// Target：你期望的接口
class IJsonParser {
public:
    virtual ~IJsonParser() = default;
    virtual bool loadFromString(const QByteArray& data) = 0;
    virtual QVariant value(const QString& key) const = 0;
};

// Adapter：桥接两者
class LegacyJsonAdapter : public IJsonParser {
public:
    bool loadFromString(const QByteArray& data) override {
        return m_legacy.parse(data.toStdString());
    }

    QVariant value(const QString& key) const override {
        return QString::fromStdString(m_legacy.getValue(key.toStdString()));
    }

private:
    LegacyJsonLib m_legacy;  // 组合 Adaptee
};
```

#### Qt 中的适配器

```cpp
// QFileSystemModel → 适配文件系统数据到 QAbstractItemModel 接口
// QSqlTableModel → 适配数据库表到 QAbstractItemModel 接口
// 这样 QTableView、QListView 不需要知道数据来源是文件系统还是数据库

QFileSystemModel fileModel;         // 文件系统（Adaptee）
QSqlTableModel  sqlModel;           // 数据库（Adaptee）

QTableView view;                    // Client，只认识 QAbstractItemModel
view.setModel(&fileModel);          // ✅ 都能工作
view.setModel(&sqlModel);           // ✅ 因为适配器统一了接口
```

### 4.2 Bridge（桥接）

> **意图**：将**抽象**与**实现**分离，使两者可以独立变化。

#### 生产场景：告警系统的"类爆炸"

你负责公司的告警通知系统。产品需求看起来很简单：

> "告警分两级——普通告警发邮件，紧急告警同时发邮件+短信+电话。"

你用继承实现：`Alert` → `EmailAlert` / `SmsAlert`，紧急的再加 `UrgentEmailAlert` / `UrgentSmsAlert`。4 种组合，还好。

然后产品加了"企业微信通知"和"钉钉通知"。紧急告警又分"严重"和"灾难"两个子级。你发现类的数量变成了：**3 个紧急等级 × 5 个发送渠道 = 15 个类**。

更崩溃的是，如果要给所有"邮件类通知"加一个"重试逻辑"，你得改 3 个类。**核心问题：两个独立变化的维度（通知级别 + 发送渠道）不应该绑在同一个继承树上。** 你需要把"变化原因不同的东西"拆到两个独立的体系里——这就是 Bridge。

```mermaid
flowchart LR
    subgraph WRONG["❌ 错误：组合爆炸"]
        N["通知"] --> N1["普通"] --> N1a["邮件"]
        N1 --> N1b["短信"]
        N --> N2["紧急"] --> N2a["邮件"]
        N2 --> N2b["短信"]
    end

    subgraph RIGHT["✅ 正确：Bridge"]
        ABS["<b>抽象层</b><br>NormalNotification<br>UrgentNotification"]
        IMPL["<b>实现层</b><br>EmailSender<br>SmsSender<br>PushSender"]
        ABS -->|桥接| IMPL
    end

    style WRONG fill:#fee2e2,stroke:#dc2626
    style RIGHT fill:#dbeafe,stroke:#2563eb
```

#### C++ 实现

```cpp
// 实现层接口：发送渠道
class IMessageSender {
public:
    virtual ~IMessageSender() = default;
    virtual void send(const QString& title, const QString& body) = 0;
};

class EmailSender : public IMessageSender {
public:
    void send(const QString& title, const QString& body) override {
        qDebug() << "[Email]" << title << body;
    }
};

class SmsSender : public IMessageSender {
public:
    void send(const QString& title, const QString& body) override {
        qDebug() << "[SMS]" << title << body;
    }
};

// 抽象层：通知类型
class Notification {
protected:
    std::unique_ptr<IMessageSender> m_sender;  // 桥接 → 实现层

public:
    Notification(std::unique_ptr<IMessageSender> sender)
        : m_sender(std::move(sender)) {}
    virtual ~Notification() = default;
    virtual void notify(const QString& msg) = 0;
};

class NormalNotification : public Notification {
public:
    using Notification::Notification;  // 继承构造函数

    void notify(const QString& msg) override {
        m_sender->send("普通通知", msg);  // 委托给实现层
    }
};

class UrgentNotification : public Notification {
public:
    using Notification::Notification;

    void notify(const QString& msg) override {
        m_sender->send("【紧急】" + msg, "请立即处理！");
    }
};

// 使用 — 两个维度可以自由组合
auto emailNotify = std::make_unique<UrgentNotification>(
    std::make_unique<EmailSender>());
emailNotify->notify("服务器磁盘使用率 95%");

auto smsNotify = std::make_unique<NormalNotification>(
    std::make_unique<SmsSender>());
smsNotify->notify("周报已生成");
```

#### Qt 中的 Bridge

```cpp
// QAbstractButton 是著名的 Bridge 应用
//   抽象层：QAbstractButton、QPushButton、QCheckBox、QRadioButton
//   实现层：QStyle（负责实际绘制）

// 相同的行为（点击、选中） + 不同的平台绘制 = Bridge 的两个维度独立变化
QPushButton btn("确定");
// 绘制委托给 QStyle（实现层），按钮自身不关心绘制细节
btn.style();  // 返回 QStyle*，即桥接的实现层
```

### 4.3 Composite（组合）

> **意图**：将对象组合成**树形结构**来表示"部分-整体"层次，使客户端能一致地对待单个对象和组合对象。

#### 生产场景：设置面板的"禁用难题"

你做了一个复杂软件的设置面板：

```
设置窗口
├── 通用设置 Tab
│   ├── 语言下拉框
│   └── 开机启动复选框
├── 网络设置 Tab
│   ├── 代理设置 GroupBox
│   │   ├── 代理地址输入框
│   │   └── 代理端口输入框
│   └── 超时时间滑块
└── 高级设置 Tab
    └── 调试模式复选框
```

产品提了一个需求："当用户取消勾选'使用代理'时，整个代理设置 GroupBox 内的所有控件都要变灰禁用。"

你第一反应：写一段代码，遍历代理 GroupBox 下的所有控件，逐个 `setEnabled(false)`。但这只能处理 GroupBox 这一层。如果未来有一个"折叠面板"也能嵌套控件呢？如果是"检查更新失败时禁用整个设置窗口的所有控件"呢？

**核心问题：你需要对"单个控件"和"包含多个控件的容器"执行相同的操作（如禁用、隐藏、保存），但它们的内部结构完全不同。** 你希望客户端代码不需要区分"个体"和"群体"——这就是 Composite。

#### 结构

```mermaid
classDiagram
    class Component {
        +getSize() qint64
    }
    class Leaf {
        +getSize() qint64
    }
    class Composite {
        -children : List~Component~
        +getSize() qint64
        +add(Component)
    }

    Component <|-- Leaf : "文件"
    Component <|-- Composite : "文件夹"
    Composite o--> Component : "children"
```

#### C++ 实现

```cpp
// 组件接口
class FileSystemNode {
public:
    virtual ~FileSystemNode() = default;
    virtual qint64 getSize() const = 0;
    virtual QString name() const = 0;
};

// Leaf：普通文件
class File : public FileSystemNode {
public:
    File(const QString& name, qint64 size) : m_name(name), m_size(size) {}

    qint64 getSize() const override { return m_size; }
    QString name() const override { return m_name; }

private:
    QString m_name;
    qint64 m_size;
};

// Composite：文件夹
class Directory : public FileSystemNode {
public:
    Directory(const QString& name) : m_name(name) {}

    void add(std::unique_ptr<FileSystemNode> node) {
        m_children.push_back(std::move(node));
    }

    qint64 getSize() const override {
        qint64 total = 0;
        for (const auto& child : m_children)
            total += child->getSize();  // 递归求和
        return total;
    }

    QString name() const override { return m_name; }

private:
    QString m_name;
    std::vector<std::unique_ptr<FileSystemNode>> m_children;
};

// 使用 — 客户端代码不需要区分文件和文件夹
auto root = std::make_unique<Directory>("root");
auto docs = std::make_unique<Directory>("docs");
docs->add(std::make_unique<File>("readme.md", 2048));
docs->add(std::make_unique<File>("guide.pdf", 15360));
root->add(std::move(docs));
root->add(std::make_unique<File>("config.json", 512));

qDebug() << root->getSize();  // 17920 — 递归汇总，客户端不关心内部结构
```

#### Qt 中的 Composite

Qt 的核心——**QObject 对象树**就是 Composite 模式最典型的应用：

```cpp
QObject* parent = new QObject;

// QObject 的 children() 存储子对象列表
auto* child1 = new QObject(parent);    // QTimer
auto* child2 = new QObject(parent);    // QNetworkAccessManager

// parent 析构时自动析构所有子对象 → Composite 的内存管理
delete parent;
// child1, child2 也会被自动 delete

// QLayout 体系也是 Composite
QVBoxLayout* mainLayout = new QVBoxLayout;
mainLayout->addLayout(new QHBoxLayout);  // 嵌套布局
mainLayout->addWidget(new QPushButton);
// QWidget::setLayout() → layout() → 递归 layout()
```

### 4.4 Decorator（装饰器）

> **意图**：动态地给对象添加额外的职责，比继承更灵活。

#### 生产场景：数据上报链的"按需组合"

你做一个 IoT 数据上报系统，设备采集到的数据在发送前需要进行多道加工：**压缩**（省流量）→ **加密**（安全）→ **添加签名**（防篡改）→ **Base64 编码**（兼容 HTTP）。

问题在于——不是所有场景都需要全部步骤：
- 内网测试环境：不需要加密，不需要签名
- 公网 Wi-Fi 设备：压缩 + 加密 + 签名，但不需要 Base64
- 蜂窝网络设备：全部都要
- 某客户要求"只加密不压缩"（他们的数据已经是压缩过的）

如果用继承来实现，`CompressedEncryptedStream`、`CompressedEncryptedSignedStream`、`EncryptedStream`...组合数 = 2^4 = 16 个子类。而且以后加一个"限速"步骤，子类又翻倍。

**核心问题：需要在**运行时**动态地、按任意顺序给一个对象附加额外功能，且每种功能可以独立开关。** 继承做不到这一点——组合在编译时就固化了。你需要一种"运行时包装"的机制。

#### 结构

```mermaid
classDiagram
    class Component {
        +write(data)
    }
    class ConcreteComponent {
        +write(data)
    }
    class Decorator {
        -inner : Component
        +write(data)
    }
    class CompressedDecorator {
        +write(data)
    }
    class EncryptedDecorator {
        +write(data)
    }

    Component <|-- ConcreteComponent
    Component <|-- Decorator
    Decorator <|-- CompressedDecorator
    Decorator <|-- EncryptedDecorator
    Decorator o--> Component : "wraps one"
    note for Decorator "持有 Component 引用，转发请求"
```

#### C++ 实现

```cpp
// 组件接口
class IDataStream {
public:
    virtual ~IDataStream() = default;
    virtual void write(const QByteArray& data) = 0;
};

// 具体组件：原始文件写入
class FileStream : public IDataStream {
public:
    FileStream(const QString& path) : m_file(path) {
        m_file.open(QIODevice::WriteOnly);
    }

    void write(const QByteArray& data) override {
        m_file.write(data);
    }

private:
    QFile m_file;
};

// 装饰器基类
class StreamDecorator : public IDataStream {
protected:
    std::unique_ptr<IDataStream> m_inner;  // 被装饰的对象

public:
    StreamDecorator(std::unique_ptr<IDataStream> inner)
        : m_inner(std::move(inner)) {}
};

// 具体装饰器：压缩
class CompressedStream : public StreamDecorator {
public:
    using StreamDecorator::StreamDecorator;

    void write(const QByteArray& data) override {
        QByteArray compressed = qCompress(data, 9);  // 先压缩
        m_inner->write(compressed);                   // 再传给下一层
    }
};

// 具体装饰器：加密
class EncryptedStream : public StreamDecorator {
public:
    using StreamDecorator::StreamDecorator;

    void write(const QByteArray& data) override {
        QByteArray encrypted = aesEncrypt(data, m_key);  // 先加密
        m_inner->write(encrypted);                        // 再传给下一层
    }

private:
    QByteArray m_key = "my-secret-key";
};

// 使用 — 像洋葱一样层层包裹
auto stream = std::make_unique<FileStream>("data.bin");
stream = std::make_unique<CompressedStream>(std::move(stream));
stream = std::make_unique<EncryptedStream>(std::move(stream));
// 调用链: Encrypted → Compressed → FileStream
stream->write(someData);
```

### 4.5 Facade（外观）

> **意图**：为子系统中的一组接口提供一个**统一的高层接口**，简化子系统的使用。

#### 生产场景：视频转码的"参数噩梦"

你给公司的短视频平台做一个转码功能。底层调 FFmpeg，命令行长这样：

```bash
ffmpeg -i input.mp4 -c:v libx264 -preset medium -crf 23 -vf "scale=1920:1080,fps=30" \
  -c:a aac -b:a 128k -ar 44100 -movflags +faststart -y output.mp4
```

每次转码都要设置十几个参数：编码器、码率、分辨率、帧率、音频采样率...而且不同场景（H5 播放、APP 高清、缩略图预览）参数完全不同。

如果让每个调用方都去拼 FFmpeg 命令行，结果就是——业务代码里散落着 20 多处 `QProcess::execute("ffmpeg -i ...")`, 每次调参数要改 8 个文件，新来的同事根本不敢动。

**核心问题：一个复杂子系统（FFmpeg / 编译工具链）有十几个类、几十个配置项，调用方只需要一个简单的入口。** "给我一个 `transcode(input, profile)` 就好，剩下的你别让我管"——这就是 Facade。

```mermaid
flowchart TD
    CLIENT["Client"]
    FACADE["<b>CompilerFacade</b><br>+ build(source)"]
    CLIENT --> FACADE
    FACADE --> PRE["Preprocessor"]
    FACADE --> COM["Compiler"]
    FACADE --> ASM["Assembler"]
    FACADE --> LINK["Linker"]

    style FACADE fill:#1e293b,color:#fff
```

#### C++ / Qt 实现

```cpp
class Preprocessor {
public:
    QString run(const QString& source) { /* 宏展开、头文件展开 */ return source; }
};

class Compiler {
public:
    QByteArray compile(const QString& preprocessed) { /* AST → IR */ return {}; }
};

class Assembler {
public:
    QByteArray assemble(const QByteArray& ir) { /* IR → 机器码 */ return {}; }
};

class Linker {
public:
    QByteArray link(const QByteArray& obj, const QList<QByteArray>& libs) { return {}; }
};

// Facade：屏蔽内部复杂性
class CompilerFacade {
public:
    QByteArray build(const QString& sourceFile) {
        QFile file(sourceFile);
        file.open(QIODevice::ReadOnly);
        QString source = file.readAll();

        QString preprocessed = m_preprocessor.run(source);
        QByteArray ir = m_compiler.compile(preprocessed);
        QByteArray obj = m_assembler.assemble(ir);
        return m_linker.link(obj, m_libraries);
    }

    void addLibrary(const QByteArray& lib) {
        m_libraries.append(lib);
    }

private:
    Preprocessor m_preprocessor;
    Compiler     m_compiler;
    Assembler    m_assembler;
    Linker       m_linker;
    QList<QByteArray> m_libraries;
};

// 使用
CompilerFacade facade;
facade.addLibrary("libstdc++.so");
QByteArray binary = facade.build("main.cpp");
```

#### Qt 中的 Facade

```cpp
// QFileDialog::getOpenFileName() 就是 Facade
// 背后调用了大量的 Windows/macOS/Linux 原生对话框 API
QString path = QFileDialog::getOpenFileName(
    nullptr, "打开文件", "", "Images (*.png *.jpg)"
);
// 一行代码搞定，不用关心平台差异

// QDesktopServices::openUrl() 同理
QDesktopServices::openUrl(QUrl("https://example.com"));
```

### 4.6 Flyweight（享元）

> **意图**：用共享来高效支持大量细粒度对象。

#### 生产场景：游戏里的"千军万马"

你做了一个 RTS 游戏，屏幕上同时有 500 个"步兵"单位。每个步兵有：位置、血量、朝向（这些都是各不相同的"外在状态"），但它们的 3D 模型、纹理贴图、移动速度、攻击动画（这些"内在状态"）完全一样。

如果你给每个步兵创建一个完整的 `Soldier` 对象——包含 80MB 的纹理数据——500 个步兵就是 40GB 内存，还没开打就 OOM 了。

**核心问题：当有海量"相似但不完全相同"的对象时，如何将不变的部分提取出来共享，只为每个对象存储变化的部分？** 就像图书馆里 1000 本《三体》共享同一份印刷版，每本只需要一个不同的"当前页码"书签。

#### C++ 实现

```cpp
// 享元：被共享的"内在状态"（不变的）
class CharacterStyle {
public:
    CharacterStyle(const QFont& font, const QColor& color)
        : m_font(font), m_color(color) {}

    void apply(QPainter& p) const {
        p.setFont(m_font);
        p.setPen(m_color);
    }

    // 用于哈希比较
    bool operator==(const CharacterStyle& o) const {
        return m_font == o.m_font && m_color == o.m_color;
    }

private:
    QFont m_font;
    QColor m_color;
};

// 哈希函数
inline size_t qHash(const CharacterStyle& s, size_t seed = 0) {
    return qHash(s.m_font.toString()) ^ qHash(s.m_color.name());
}

// 享元工厂
class StyleFactory {
public:
    const CharacterStyle& getStyle(const QFont& font, const QColor& color) {
        CharacterStyle key(font, color);
        auto it = m_pool.find(key);
        if (it != m_pool.end())
            return *it;  // 返回已有的共享对象

        auto* style = new CharacterStyle(font, color);
        m_pool.insert(style);
        return *style;
    }

    ~StyleFactory() { qDeleteAll(m_pool); }

private:
    QSet<CharacterStyle*> m_pool;
};

// 使用：每个字符只存储"外在状态"（位置、字符内容）+ 指向共享格式的指针
struct Character {
    QChar ch;
    int x, y;
    const CharacterStyle* style;  // 指向共享的格式对象
};
```

#### Qt 中的 Flyweight

```cpp
// QFontCache / QPixmapCache 是享元模式的实例
QPixmapCache::insert("icon-home", QPixmap(":/icons/home.png"));
// 之后按 key 获取，不会重复加载

// QIcon 内部也共享像素数据
QIcon icon(":/icons/home.png");
auto sizes = icon.availableSizes();  // 不同尺寸共享同一份源数据
```

### 4.7 Proxy（代理）

> **意图**：为另一个对象提供一个**替身**或**占位符**，以控制对它的访问。

#### 生产场景：相册的"秒开"秘密

你做了一个云相册 APP。用户打开相册时看到的是缩略图网格，点进去才看原图。一开始你用了最直接的方案：

```cpp
QPixmap fullImage("https://cdn.example.com/photos/img_2024.jpg");
// 下载 12MB 的原图 → 界面卡了 3 秒 → 用户划走了
```

后来你改成"先显示缩略图，点击后再下载原图"。问题是——你怎么让网格组件"认为"它拿到的就是最终图片对象？如果让网格组件自己去判断"这是缩略图还是原图"，组件就耦合了加载逻辑。

**核心问题：需要一个与原对象"一模一样"的替身，让调用方完全无感——但替身在背后可以做延迟加载、权限检查、缓存、远程调用等额外操作。** 这就是 Proxy 的精髓：同样的接口，不同的背后行为。

#### 变体

| 代理类型 | 用途 |
|---------|------|
| **Virtual Proxy（虚拟代理）** | 延迟创建开销大的对象 |
| **Remote Proxy（远程代理）** | 代表位于不同地址空间的对象 |
| **Protection Proxy（保护代理）** | 控制访问权限 |
| **Smart Reference（智能引用）** | 在访问对象时附加额外操作（引用计数、日志） |

#### C++ 实现（虚拟代理）

```cpp
// Subject 接口
class IImage {
public:
    virtual ~IImage() = default;
    virtual void display(QPainter& p, int x, int y) = 0;
    virtual QSize size() const = 0;
};

// RealSubject：真正加载图片（开销大）
class HighResImage : public IImage {
public:
    explicit HighResImage(const QString& path)
        : m_pixmap(path)  // 加载可能很慢，占内存很大
    {
        qDebug() << "Loading image from disk:" << path;
    }

    void display(QPainter& p, int x, int y) override {
        p.drawPixmap(x, y, m_pixmap);
    }

    QSize size() const override { return m_pixmap.size(); }

private:
    QPixmap m_pixmap;
};

// Proxy：延迟加载
class LazyImageProxy : public IImage {
public:
    explicit LazyImageProxy(const QString& path)
        : m_path(path) {}

    void display(QPainter& p, int x, int y) override {
        ensureLoaded();
        m_realImage->display(p, x, y);
    }

    QSize size() const override {
        ensureLoaded();
        return m_realImage->size();
    }

private:
    void ensureLoaded() const {
        if (!m_realImage)
            m_realImage = std::make_unique<HighResImage>(m_path);
    }

    QString m_path;
    mutable std::unique_ptr<HighResImage> m_realImage;  // 可变的缓存
};

// 使用
LazyImageProxy img("huge-photo.jpg");
// 此时还没有加载文件，内存占用极低
// 只有当真正需要绘制时才去加载
```

#### Qt 中的 Proxy

```cpp
// QSortFilterProxyModel — 代理模式 + 额外过滤/排序功能
QSortFilterProxyModel proxy;
proxy.setSourceModel(originalModel);  // 代理原始模型
proxy.setFilterRegExp(QRegExp("*.cpp"));  // 额外功能：过滤

QTableView view;
view.setModel(&proxy);  // view 不需要知道数据被过滤了

// QNetworkAccessManager 的缓存机制也是代理
QNetworkRequest req(url);
req.setAttribute(QNetworkRequest::CacheLoadControlAttribute,
                 QNetworkRequest::PreferCache);
```

---

## 5. 行为型模式（Behavioral Patterns）

行为型模式关注对象之间的通信和职责分配。

### 5.1 Chain of Responsibility（责任链）

> **意图**：将请求沿一条处理者链传递，直到有处理者处理它。

#### 生产场景：报销审批流的"硬编码地狱"

你给一家公司做 OA 系统，报销审批规则如下：

- 500 元以下：组长审批即可
- 500 ~ 5000 元：组长 → 部门经理
- 5000 元以上：组长 → 部门经理 → 财务总监

你第一版代码：

```cpp
if (amount < 500) {
    leader.approve(request);
} else if (amount < 5000) {
    leader.approve(request);
    manager.approve(request);
} else {
    leader.approve(request);
    manager.approve(request);
    cfo.approve(request);
}
```

上线一周后，HR 说"金额超 1 万的还要 CEO 批"。你又加了一个 `else if`。然后财务说"报销类型如果是差旅费，不论金额多少都只要组长和行政两个审批"。审批人还可能请假，需要设置代理人...

每次规则变化，你都要在 `approve()` 方法里翻找对应的 `if-else` 分支。更糟的是，"组长驳回"和"部门经理驳回"要发不同的通知，这些逻辑全部散落在同一个巨大的方法里。

**核心问题：请求需要沿着一个有顺序的处理链传递，每个处理者可以"处理"或"传递"，链的结构需要在运行时灵活调整。** 写死在 `if-else` 里等于把变化最频繁的业务规则钉死了。

```mermaid
flowchart LR
    REQ["Request"] --> AUTH["认证"]
    AUTH -->|"pass"| PERM["权限"]
    AUTH -->|"reject"| E403["403"]
    PERM -->|"pass"| LIMIT["限流"]
    PERM -->|"reject"| E403
    LIMIT -->|"pass"| BIZ["业务处理"]
    LIMIT -->|"reject"| E429["429"]
```

#### C++ 实现

```cpp
class HttpRequest {
public:
    QString token;
    QString userId;
    QString path;
};

// 处理者基类
class Handler {
protected:
    Handler* m_next = nullptr;

public:
    virtual ~Handler() = default;

    Handler& setNext(Handler& next) {
        m_next = &next;
        return next;  // 返回引用以便链式调用
    }

    virtual void handle(HttpRequest& req) {
        if (m_next)
            m_next->handle(req);  // 默认：传给下一个
    }
};

class AuthHandler : public Handler {
public:
    void handle(HttpRequest& req) override {
        if (req.token.isEmpty()) {
            qDebug() << "403: No token";
            return;  // 中断链
        }
        qDebug() << "Auth passed";
        Handler::handle(req);  // 传给下一个
    }
};

class ThrottleHandler : public Handler {
public:
    void handle(HttpRequest& req) override {
        if (m_requestCount > 100) {
            qDebug() << "429: Too many requests";
            return;
        }
        m_requestCount++;
        qDebug() << "Throttle passed";
        Handler::handle(req);
    }

private:
    int m_requestCount = 0;
};

// 使用 — 链式组装
AuthHandler auth;
ThrottleHandler throttle;

auth.setNext(throttle);  // auth → throttle

HttpRequest req;
req.token = "valid-token";
auth.handle(req);  // 从链头开始
```

#### Qt 中的责任链

Qt 的**事件系统**就是责任链——事件从子 widget 向父 widget 传递，直到被处理：

```cpp
// QWidget 事件传播链：target → parent → grandparent → ...
// 每个 widget 都可以处理或忽略事件
void MyWidget::mousePressEvent(QMouseEvent* event) {
    if (canHandle(event)) {
        // 处理它 → 链终止
    } else {
        QWidget::mousePressEvent(event);  // 传给父类（链上的下一个）
    }
}
```

### 5.2 Command（命令）

> **意图**：将**请求封装为对象**，从而支持参数化、队列、日志和撤销操作。

#### 生产场景：文本编辑器的"Ctrl+Z"

你正在写一个 Markdown 编辑器。用户做了一个操作链：

```
1. 删除了一段文字
2. 把标题加粗
3. 插入了一张图片
4. 删除了一个段落
```

现在用户按 `Ctrl+Z` 4 次，期望依次撤销上述操作。如果你的代码是：

```cpp
void onDeleteText(int pos, int len) {
    doc->remove(pos, len);  // 删了就没了，怎么恢复？
}
```

**一个方法调用完成后，执行信息就消失了**——它不保存"删了什么、在哪个位置删的"。更不用说"重做"（Ctrl+Y）：撤销了又想恢复，需要"反撤销"。

**核心问题：如何把"操作"本身变成对象——存储操作名称、参数、执行前状态——使得可以放入历史队列、回滚、甚至序列化到磁盘？** 不是"调用一个方法"，而是"创建一个命令对象，放入队列，执行它，保存它"。

#### 结构

```mermaid
classDiagram
    class Client {
    }
    class Invoker {
    }
    class Command {
        +execute()
        +undo()
    }
    class ConcreteCommand {
        -state
        +execute()
        +undo()
    }
    class Receiver {
        +action()
    }

    Client --> ConcreteCommand : creates
    Invoker --> Command : holds
    Command <|-- ConcreteCommand
    ConcreteCommand --> Receiver : calls
```

#### C++ 实现（撤销/重做）

```cpp
// Receiver：真正执行操作的对象
class TextEditor {
public:
    void insertText(int pos, const QString& text) {
        m_content.insert(pos, text);
    }

    void deleteText(int pos, int length) {
        m_deleted = m_content.mid(pos, length);
        m_content.remove(pos, length);
    }

    QString content() const { return m_content; }

private:
    QString m_content;
    QString m_deleted;
};

// Command 基类
class Command {
public:
    virtual ~Command() = default;
    virtual void execute() = 0;
    virtual void undo() = 0;
};

// 具体命令：插入文本
class InsertCommand : public Command {
public:
    InsertCommand(TextEditor& editor, int pos, const QString& text)
        : m_editor(editor), m_pos(pos), m_text(text) {}

    void execute() override {
        m_editor.insertText(m_pos, m_text);
    }

    void undo() override {
        m_editor.deleteText(m_pos, m_text.length());
    }

private:
    TextEditor& m_editor;
    int m_pos;
    QString m_text;
};

// Invoker：管理命令历史
class CommandHistory {
public:
    void execute(std::unique_ptr<Command> cmd) {
        cmd->execute();
        m_undoStack.push_back(std::move(cmd));
        m_redoStack.clear();  // 新命令清空重做栈
    }

    void undo() {
        if (m_undoStack.empty()) return;
        auto& cmd = m_undoStack.back();
        cmd->undo();
        m_redoStack.push_back(std::move(cmd));
        m_undoStack.pop_back();
    }

    void redo() {
        if (m_redoStack.empty()) return;
        auto& cmd = m_redoStack.back();
        cmd->execute();
        m_undoStack.push_back(std::move(cmd));
        m_redoStack.pop_back();
    }

private:
    std::vector<std::unique_ptr<Command>> m_undoStack;
    std::vector<std::unique_ptr<Command>> m_redoStack;
};

// 使用
TextEditor editor;
CommandHistory history;
editor.insertText(0, "Hello");
history.execute(std::make_unique<InsertCommand>(editor, 5, " World"));
// editor.content() == "Hello World"
history.undo();
// editor.content() == "Hello"
history.redo();
// editor.content() == "Hello World"
```

#### Qt 中的 Command

Qt 的 **QUndoStack / QUndoCommand** 就是 Command 模式的完整实现：

```cpp
class InsertTextCmd : public QUndoCommand {
public:
    InsertTextCmd(QTextDocument* doc, int pos, const QString& text)
        : m_doc(doc), m_pos(pos), m_text(text) {
        setText("插入文本");  // 在 Undo 菜单中显示
    }

    void redo() override {
        QTextCursor cursor(m_doc);
        cursor.setPosition(m_pos);
        cursor.insertText(m_text);
    }

    void undo() override {
        QTextCursor cursor(m_doc);
        cursor.setPosition(m_pos);
        cursor.movePosition(QTextCursor::Right, QTextCursor::KeepAnchor,
                            m_text.length());
        cursor.removeSelectedText();
    }

private:
    QTextDocument* m_doc;
    int m_pos;
    QString m_text;
};

QUndoStack undoStack;
undoStack.push(new InsertTextCmd(doc, 0, "Hello Qt"));
// 自动支持 Undo/Redo 快捷键和菜单集成
```

### 5.3 Iterator（迭代器）

> **意图**：提供一种方法**顺序访问**聚合对象中的各个元素，而**不暴露其内部表示**。

#### C++ / Qt 实现

C++ STL 和 Qt 的迭代器是这个模式最经典的实现：

```cpp
// STL 风格
std::vector<int> vec = {1, 2, 3, 4, 5};
for (auto it = vec.begin(); it != vec.end(); ++it) {
    qDebug() << *it;
}

// Qt 风格 — Java 风格迭代器（更安全）
QList<QString> list = {"a", "b", "c"};
QListIterator<QString> it(list);
while (it.hasNext()) {
    qDebug() << it.next();
}

// Qt 风格 — STL 风格迭代器（更高效）
for (auto it = list.begin(); it != list.end(); ++it) {
    *it = it->toUpper();
}

// C++11 range-for（语法糖，底层用 begin()/end()）
for (const auto& item : list) {
    qDebug() << item;
}
```

#### 自定义迭代器

```cpp
// 自定义可迭代容器
template<typename T>
class TreeNode {
public:
    T data;
    std::vector<std::unique_ptr<TreeNode>> children;

    // 前序遍历迭代器（简化示意）
    class PreOrderIterator {
        std::vector<TreeNode*> m_stack;

    public:
        explicit PreOrderIterator(TreeNode* root) {
            if (root) m_stack.push_back(root);
        }

        T& operator*() { return m_stack.back()->data; }
        PreOrderIterator& operator++() {
            auto* node = m_stack.back();
            m_stack.pop_back();
            // 逆序压入子节点
            for (auto it = node->children.rbegin();
                 it != node->children.rend(); ++it) {
                m_stack.push_back(it->get());
            }
            return *this;
        }
        bool operator!=(const PreOrderIterator&) const {
            return !m_stack.empty();
        }
    };

    PreOrderIterator begin() { return PreOrderIterator(this); }
    PreOrderIterator end()   { return PreOrderIterator(nullptr); }
};
```

### 5.4 Mediator（中介者）

> **意图**：用一个**中介对象**来封装一组对象之间的交互，避免它们之间显式的相互引用。

#### 生产场景：智能家居的"联动失控"

你给一个智能家居系统写控制逻辑。场景：离家模式。

```
门锁上锁 → 所有灯光关闭 → 窗帘拉上 → 空调关闭 → 摄像头开启 → 扫地机器人开始工作
```

你让"门锁对象"直接调用"灯光对象"的方法，灯光再调用窗帘，窗帘再调空调...过了一周，产品加了"回家模式"——逻辑几乎全部反过来。又过了一周，"睡眠模式"要求：关灯 + 拉窗帘 + 开空调(26°C) + 不要扫地。

你发现每个设备对象里都引用了 5 个其他设备的指针，改一个设备的逻辑，不小心就动了其他模式的逻辑。**核心问题：多个对象之间的交互关系是一个复杂的网络——你需要的不是让对象互相引用，而是引入一个"协调中心"，所有对象只和它通信。** 就像机场塔台：飞机之间不直接对话，都听塔台调度。

```mermaid
flowchart LR
    subgraph WRONG["❌ 没有中介者：网状依赖"]
        A1["A"] --> B1["B"]
        A1 --> C1["C"]
        A1 --> D1["D"]
        B1 --> A1
        B1 --> C1
        B1 --> E1["E"]
        C1 --> A1
        C1 --> D1
    end

    subgraph RIGHT["✅ 有中介者：星型结构"]
        A2["A"] --> MED["Mediator"]
        B2["B"] --> MED
        C2["C"] --> MED
        MED -->|"协调"| A2
        MED -->|"协调"| B2
        MED -->|"协调"| C2
    end

    style WRONG fill:#fee2e2,stroke:#dc2626
    style RIGHT fill:#dbeafe,stroke:#2563eb
```

#### C++ / Qt 实现

```cpp
// Mediator 接口
class DialogMediator {
public:
    virtual ~DialogMediator() = default;
    virtual void notify(QWidget* sender, const QString& event) = 0;
};

// 具体中介者：管理对话框各控件之间的交互逻辑
class LoginDialogMediator : public DialogMediator {
public:
    LoginDialogMediator(QLineEdit* username, QLineEdit* password,
                        QCheckBox* remember, QPushButton* login)
        : m_username(username), m_password(password),
          m_remember(remember), m_login(login)
    {
        // 安装事件过滤器来监听各控件变化
        m_username->installEventFilter(this);
        m_password->installEventFilter(this);
        m_remember->installEventFilter(this);
    }

    void notify(QWidget* sender, const QString& event) override {
        if (sender == m_remember) {
            // 记住密码：预填充上次的用户名
            if (m_remember->isChecked()) {
                m_username->setText(m_savedUsername);
            }
        }
        // 每次输入变化都检查登录按钮是否可点
        if (sender == m_username || sender == m_password) {
            m_login->setEnabled(
                !m_username->text().isEmpty() &&
                !m_password->text().isEmpty()
            );
        }
    }

private:
    QLineEdit* m_username;
    QLineEdit* m_password;
    QCheckBox* m_remember;
    QPushButton* m_login;
    QString m_savedUsername;
};
```

#### Qt 中的 Mediator

对话框类本身就是天然的中介者，而 Qt 的**信号槽机制**在很多场景下可以替代中介者：

```cpp
// 信号槽自动充当 Mediator：各控件不直接引用彼此
connect(checkBox, &QCheckBox::toggled, this, [this](bool checked) {
    lineEdit->setEnabled(!checked);  // 中介逻辑在对话框类中
    updateLoginButton();
});
connect(lineEdit, &QLineEdit::textChanged, this, &LoginDialog::updateLoginButton);
```

### 5.5 Memento（备忘录）

> **意图**：在不破坏封装性的前提下，**捕获并外部化**一个对象的内部状态，以便后续恢复。

#### 生产场景：文档编辑器的"崩溃恢复"

你做一个在线文档应用。用户写了 3000 字，浏览器突然崩溃了。重启后，你希望能恢复到崩溃前的状态——就像什么都没发生过。

最直接的想法：每隔 30 秒把整个文档对象序列化存到 `localStorage`。但文档对象内部有几十个私有字段（光标位置、选区范围、样式缓存、Undo 栈...），如果全部暴露出来，外部就能随意修改这些内部状态，破坏了封装。

**核心问题：如何在"不暴露对象内部细节"的前提下，捕获并外部化对象的完整状态，以便后续恢复？** 就像游戏存档——游戏角色不知道存档文件长什么样，存档管理器不知道角色有哪些属性，只有角色自己能"读档"和"存档"。

#### C++ 实现

```cpp
// Memento：不可变的状态快照
class PlayerMemento {
    friend class Player;  // 只有 Player 可以访问内部细节

    int m_x, m_y;
    int m_hp;
    int m_level;

    PlayerMemento(int x, int y, int hp, int level)
        : m_x(x), m_y(y), m_hp(hp), m_level(level) {}
};

// Originator：需要保存状态的对象
class Player {
public:
    void move(int dx, int dy) { m_x += dx; m_y += dy; }
    void takeDamage(int dmg) { m_hp -= dmg; }

    // 创建存档
    PlayerMemento save() const {
        return PlayerMemento(m_x, m_y, m_hp, m_level);
    }

    // 恢复存档
    void restore(const PlayerMemento& m) {
        m_x = m.m_x;
        m_y = m.m_y;
        m_hp = m.m_hp;
        m_level = m.m_level;
    }

    void print() const {
        qDebug() << "Pos:" << m_x << m_y
                 << "HP:" << m_hp << "Lv:" << m_level;
    }

private:
    int m_x = 0, m_y = 0;
    int m_hp = 100;
    int m_level = 1;
};

// Caretaker：管理者，不查看备忘录内容
class SaveManager {
public:
    void save(const Player& player, int slot) {
        m_saves[slot] = player.save();
    }

    bool load(Player& player, int slot) {
        auto it = m_saves.find(slot);
        if (it == m_saves.end()) return false;
        player.restore(it->second);
        return true;
    }

private:
    QMap<int, PlayerMemento> m_saves;  // 不关心内部结构
};

// 使用
Player player;
player.move(10, 20);

SaveManager sm;
sm.save(player, 1);        // 存档
player.takeDamage(30);
sm.load(player, 1);        // 读档 → 恢复
```

### 5.6 Observer（观察者）

> **意图**：定义对象间的一对多依赖关系，当一个对象**状态改变**时，所有依赖它的对象自动收到通知。

#### 生产场景：股票行情终端的"数据不同步"

你做一个股票看盘软件。界面有 5 个面板：K 线图、分时图、五档盘口、自选列表、成交明细。它们的数据都来自同一个 WebSocket 推送：

```
WebSocket → 收到 {"code":"000001","price":12.58}
```

最简单的实现：在 WebSocket 的回调里写：

```cpp
void onStockData(const StockData& data) {
    kLineWidget->updatePrice(data);      // K 线图刷新
    timeLineWidget->updatePrice(data);   // 分时图刷新
    depthWidget->updatePrice(data);      // 盘口刷新
    watchlistWidget->updatePrice(data);  // 自选列表刷新
    tradeWidget->updatePrice(data);      // 成交明细刷新
}
```

看起来直截了当。问题是——下周产品说"加一个涨速排行榜面板"。你在以上代码里又加了一行 `rankingWidget->updatePrice(data)`。一个月后你加了 8 个新面板，`onStockData` 里有 13 个方法调用。每加一个面板要改数据推送模块的代码——**数据源不应该知道有多少人在"看"它**。

**核心问题：当一个对象的状态变化时，如何自动通知所有依赖它的对象，而它不需要知道具体有哪些对象、它们各自怎么处理？** 就像微信公众号——你订阅了，它更新你就收到通知；它不需要知道你是谁。

#### 结构

```mermaid
classDiagram
    class Subject {
        +attach(Observer)
        +detach(Observer)
        +notify()
    }
    class Observer {
        +update()
    }
    class ConcreteSubject {
    }
    class ConcreteObserverA["TableView"] {
        +update()
    }
    class ConcreteObserverB["ChartView"] {
        +update()
    }

    Subject <|-- ConcreteSubject
    Observer <|-- ConcreteObserverA
    Observer <|-- ConcreteObserverB
    Subject --> Observer : notifies
    ConcreteSubject --> ConcreteObserverA : calls update()
    ConcreteSubject --> ConcreteObserverB : calls update()
```

#### Qt 实现（信号槽）

Qt 的信号槽就是 Observer 模式的巅峰实现，无需手写 Subject/Observer 类：

```cpp
// Subject：数据源
class DataSource : public QObject {
    Q_OBJECT

public:
    void updateData(const QVariantList& data) {
        if (m_data != data) {
            m_data = data;
            emit dataChanged(m_data);  // 状态改变 → 通知所有观察者
        }
    }

signals:
    void dataChanged(const QVariantList& data);

private:
    QVariantList m_data;
};

// Observers
class TableView : public QObject {
    Q_OBJECT
public slots:
    void onDataChanged(const QVariantList& data) {
        qDebug() << "Table refreshing with" << data.size() << "rows";
    }
};

class ChartView : public QObject {
    Q_OBJECT
public slots:
    void onDataChanged(const QVariantList& data) {
        qDebug() << "Chart redrawing";
    }
};

// 使用
DataSource source;
TableView table;
ChartView chart;

QObject::connect(&source, &DataSource::dataChanged,
                 &table, &TableView::onDataChanged);
QObject::connect(&source, &DataSource::dataChanged,
                 &chart, &ChartView::onDataChanged);

source.updateData({{"CPU", 85}, {"RAM", 60}});
// 两个视图自动收到通知
```

#### Qt 的其他 Observer 实现

```cpp
// QFileSystemWatcher — 观察文件系统变化
QFileSystemWatcher watcher;
watcher.addPath("/var/log/app.log");
QObject::connect(&watcher, &QFileSystemWatcher::fileChanged,
                 [](const QString& path) {
    qDebug() << "Log file changed!";
});

// QAbstractItemModel + QAbstractItemView — 模型数据变化时自动刷新视图
// Model = Subject, View = Observer
model->setData(index, newValue);  // Model 发出 dataChanged 信号
// → 所有关联的 View 自动重绘
```

### 5.7 State（状态）

> **意图**：允许对象在其**内部状态改变**时改变它的行为，看起来好像对象修改了它的类。

#### 生产场景：订单状态机的"if-else 失控"

你做一个电商订单系统。一个订单有 6 个状态：待支付 → 已支付 → 已发货 → 已签收 → 已完成 / 已取消。

每个状态下，同样的操作有不同的行为：

| 操作 | 待支付 | 已支付 | 已发货 | 已完成 |
|------|--------|--------|--------|--------|
| `cancel()` | 允许，退款 0 | 允许，全额退款 | 允许，扣除运费 | 不允许 |
| `refund()` | 不允许 | 允许 | 允许(需退货) | 允许(7天内) |
| `modifyAddress()` | 允许 | 允许 | 不允许 | 不允许 |

如果用一个 `Order` 类加状态标志位：

```cpp
void Order::cancel() {
    if (m_status == PENDING) { /* 退款0 */ }
    else if (m_status == PAID) { /* 全额退款 */ }
    else if (m_status == SHIPPED) { /* 扣运费 */ }
    else if (m_status == COMPLETED) { /* 不允许，弹提示 */ }
    // ...每个方法里都有这坨 if-else
}
```

新增一个状态（比如"部分发货"），`cancel()`、`refund()`、`modifyAddress()` 全部要加分支。**核心问题：对象的行为应该随着内部状态切换而整体改变，而不是在每个方法里用 if-else 去区分。** 理想情况是：`Order` 持有一个 `State` 对象，`cancel()` 直接委托给当前状态去处理——在"待支付状态"的 `cancel()` 和"已完成状态"的 `cancel()` 是完全不同的代码路径。

```mermaid
stateDiagram-v2
    [*] --> CLOSED
    CLOSED --> ESTABLISHED : connect()
    ESTABLISHED --> CLOSED : close()
    ESTABLISHED --> ESTABLISHED : send()

    note right of CLOSED : connect() → 建立连接<br>close()/send() → 无效
    note right of ESTABLISHED : send() → 发送数据<br>connect() → 无效
```

#### C++ 实现

```cpp
// State 基类
class TcpState {
public:
    virtual ~TcpState() = default;
    virtual void connect(TcpConnection* conn) { qDebug() << "Invalid in" << name(); }
    virtual void close(TcpConnection* conn)    { qDebug() << "Invalid in" << name(); }
    virtual void send(TcpConnection* conn, const QByteArray&) {
        qDebug() << "Invalid in" << name();
    }
    virtual QString name() const = 0;
};

// 具体状态
class ClosedState : public TcpState {
public:
    void connect(TcpConnection* conn) override;
    QString name() const override { return "CLOSED"; }
};

class EstablishedState : public TcpState {
public:
    void close(TcpConnection* conn) override;
    void send(TcpConnection* conn, const QByteArray& data) override;
    QString name() const override { return "ESTABLISHED"; }
};

// Context：持有当前状态
class TcpConnection {
    friend class ClosedState;
    friend class EstablishedState;

public:
    TcpConnection() : m_state(std::make_unique<ClosedState>()) {}

    void connect()   { m_state->connect(this); }
    void close()     { m_state->close(this); }
    void send(const QByteArray& data) { m_state->send(this, data); }

private:
    void changeState(std::unique_ptr<TcpState> newState) {
        qDebug() << m_state->name() << "→" << newState->name();
        m_state = std::move(newState);
    }

    std::unique_ptr<TcpState> m_state;
};

// 状态转换逻辑
void ClosedState::connect(TcpConnection* conn) {
    qDebug() << "建立连接...";
    conn->changeState(std::make_unique<EstablishedState>());
}

void EstablishedState::close(TcpConnection* conn) {
    qDebug() << "关闭连接...";
    conn->changeState(std::make_unique<ClosedState>());
}

void EstablishedState::send(TcpConnection* conn, const QByteArray& data) {
    qDebug() << "发送数据:" << data.size() << "bytes";
    // 实际发送逻辑
}

// 使用
TcpConnection conn;
conn.send("hello");   // "Invalid in CLOSED"
conn.connect();       // "CLOSED → ESTABLISHED"
conn.send("hello");   // "发送数据: 5 bytes"
conn.close();         // "ESTABLISHED → CLOSED"
```

#### Qt 中的 State

```cpp
// QStateMachine — Qt 内置的状态机框架，直接实现了 State 模式
QStateMachine machine;

QState* closed = new QState();
QState* established = new QState();

closed->addTransition(connectButton, &QPushButton::clicked, established);
established->addTransition(disconnectButton, &QPushButton::clicked, closed);

// 进入/退出状态时的行为
established->assignProperty(statusLabel, "text", "已连接");
closed->assignProperty(statusLabel, "text", "已断开");

machine.addState(closed);
machine.addState(established);
machine.setInitialState(closed);
machine.start();
```

### 5.8 Strategy（策略）

> **意图**：定义一系列算法，把它们封装起来，并使它们可以**互相替换**。

#### 生产场景：电商大促的"算法混战"

你给一个电商系统做促销引擎。一年下来，产品提了几十种活动规则：

- 双11：满 200 减 30，可跨店
- 618：全场 8 折，部分商品不参与
- 新人专享：首单减 10 元
- 会员日：买三免一（取价格最低的）
- 直播间：主播发优惠券，满 99 减 15
- 清仓：买一送一

如果每个活动都写一个 `if (activityType == "double11") { ... } else if (...)`，`checkout()` 方法很快会变成 500 行的 if-else 迷宫。更麻烦的是——同一个商品可能同时参与多个活动，需要计算"最优组合"；运营还经常临时调整活动规则。

**核心问题：同一种操作（"计算价格"）有多种算法实现，算法需要在运行时灵活切换甚至组合。** 每新增一种算法，应该只需加一个新类，不动已有代码。这就是 Strategy——把"会变化的算法"从业务逻辑中抽离出来，变成可插拔的策略对象。

#### 结构

```mermaid
classDiagram
    class Context {
        -strategy : Strategy
        +setStrategy()
        +checkout()
    }
    class Strategy {
        +calculate(price) double
    }
    class FullReduction {
        +calculate(price) double
    }
    class Discount {
        +calculate(price) double
    }
    class GiftPromotion {
        +calculate(price) double
    }

    Context --> Strategy
    Strategy <|-- FullReduction : 满减
    Strategy <|-- Discount : 折扣
    Strategy <|-- GiftPromotion : 买赠
```

#### C++ 实现

```cpp
// 策略接口
class IPromotionStrategy {
public:
    virtual ~IPromotionStrategy() = default;
    virtual double calculate(double originalPrice) const = 0;
    virtual QString name() const = 0;
};

// 具体策略：满减
class FullReduction : public IPromotionStrategy {
public:
    FullReduction(double threshold, double reduction)
        : m_threshold(threshold), m_reduction(reduction) {}

    double calculate(double price) const override {
        int times = static_cast<int>(price / m_threshold);
        return price - times * m_reduction;
    }

    QString name() const override {
        return QString("满%1减%2").arg(m_threshold).arg(m_reduction);
    }

private:
    double m_threshold;
    double m_reduction;
};

// 具体策略：折扣
class Discount : public IPromotionStrategy {
public:
    Discount(double rate) : m_rate(rate) {}  // rate = 0.8 即 8 折

    double calculate(double price) const override {
        return price * m_rate;
    }

    QString name() const override {
        return QString("%1折").arg(m_rate * 10);
    }

private:
    double m_rate;
};

// Context
class Order {
public:
    void setStrategy(std::unique_ptr<IPromotionStrategy> strategy) {
        m_strategy = std::move(strategy);
    }

    double checkout(double originalPrice) const {
        if (m_strategy) {
            double final = m_strategy->calculate(originalPrice);
            qDebug() << m_strategy->name() << ":"
                     << originalPrice << "→" << final;
            return final;
        }
        return originalPrice;
    }

private:
    std::unique_ptr<IPromotionStrategy> m_strategy;
};

// 使用
Order order;
order.setStrategy(std::make_unique<FullReduction>(200, 30));
order.checkout(500);  // "满200减30: 500 → 440"

order.setStrategy(std::make_unique<Discount>(0.85));
order.checkout(500);  // "8.5折: 500 → 425"
```

#### Qt 中的 Strategy

```cpp
// QCompleter 的完成模式就是策略模式
QCompleter* completer = new QCompleter(wordList, this);
completer->setCompletionMode(QCompleter::PopupCompletion);    // 策略 A
completer->setCompletionMode(QCompleter::InlineCompletion);   // 策略 B
completer->setCompletionMode(QCompleter::UnfilteredPopupCompletion); // 策略 C

// QValidator 子类也是策略：IntValidator, DoubleValidator, RegExpValidator
QLineEdit edit;
edit.setValidator(new QIntValidator(0, 100));      // 策略 A：整数
edit.setValidator(new QDoubleValidator(0, 99.9, 2)); // 策略 B：浮点数
edit.setValidator(new QRegExpValidator(QRegExp("[A-Z]+"))); // 策略 C：正则
```

### 5.9 Template Method（模板方法）

> **意图**：在一个方法中定义算法的**骨架**，将一些步骤**延迟到子类**实现。

#### 生产场景：数据导入的"复制粘贴"

你做一个 BI 系统，需要支持 3 种数据导入方式：CSV 上传、Excel 上传、从 API 拉取 JSON。

第一次实现 CSV 导入：

```cpp
bool importCSV(const QString& path) {
    auto raw = readFile(path);           // 1. 读文件
    auto valid = validateCSV(raw);       // 2. 校验
    auto rows = parseCSV(valid);         // 3. 解析
    saveToDB(rows);                      // 4. 入库
    log("import done");                  // 5. 记录日志
    return true;
}
```

然后实现 Excel 导入——流程一模一样，只是步骤 1 用了 `ExcelReader` 而非 `CSVParser`。你又写了一份几乎相同的代码，只有少数几行不同。接着 JSON API 导入也来了...

3 种导入方式，3 份高度重复的代码。如果发现"入库前需要做数据去重"，你得到 3 个地方加。**核心问题：一系列操作的**骨架**是固定的，但每个步骤的具体实现因场景而异。** 你需要把骨架定义在基类中，只把"会变化的步骤"留给子类填空——就像"考试答题卡"：题型和顺序都一样，每个学生只需在不同位置写下不同答案。

#### 结构

```mermaid
classDiagram
    class DataExporter {
        +export() final
        #prepareData()*
        #format()*
        #writeFile()*
        #cleanup()
    }
    class PdfExporter {
        #prepareData()
        #format()
        #writeFile()
        #cleanup()
    }
    class CsvExporter {
        #prepareData()
        #format()
        #writeFile()
    }

    DataExporter <|-- PdfExporter
    DataExporter <|-- CsvExporter
    note for DataExporter "export() 是 template method（final）<br>prepareData/format/writeFile 是原语（=0）<br>cleanup 是钩子（可选覆盖）"
```

#### C++ 实现

```cpp
class DataExporter {
public:
    virtual ~DataExporter() = default;

    // 模板方法 — final 禁止子类覆盖
    void exportData(const QList<QStringList>& data, const QString& path) final {
        m_data = data;
        m_path = path;

        qDebug() << "=== 开始导出 ===";
        prepareData();
        format();
        writeFile();
        cleanup();
        qDebug() << "=== 导出完成 ===";
    }

protected:
    virtual void prepareData() = 0;
    virtual void format() = 0;
    virtual void writeFile() = 0;

    // 钩子：有默认空实现，子类可以选择性覆盖
    virtual void cleanup() {
        m_data.clear();  // 默认清理数据
    }

    QList<QStringList> m_data;
    QString m_path;
};

class CsvExporter : public DataExporter {
protected:
    void prepareData() override {
        qDebug() << "CSV: Preparing data...";
    }

    void format() override {
        qDebug() << "CSV: Formatting rows...";
    }

    void writeFile() override {
        QFile file(m_path);
        file.open(QIODevice::WriteOnly);
        QTextStream stream(&file);
        for (const auto& row : m_data) {
            stream << row.join(',') << "\n";
        }
    }
};

class PdfExporter : public DataExporter {
protected:
    void prepareData() override { /* 构建 PDF 文档结构 */ }
    void format() override      { /* 设置字体、页面布局 */ }
    void writeFile() override   { QPdfWriter writer(m_path); /* ... */ }

    void cleanup() override {
        m_tempFiles.clear();  // 额外的清理：删除临时文件
        DataExporter::cleanup();
    }

private:
    QStringList m_tempFiles;
};
```

#### Qt 中的 Template Method

```cpp
// QThread::run() — 模板方法的核心
// start() 内部调用 run()，子类只需重写 run()
class WorkerThread : public QThread {
protected:
    void run() override {  // 实现算法步骤
        // 模板方法 start() 会调用这里
        while (!isInterruptionRequested()) {
            doWork();
            msleep(100);
        }
    }
};

// QAbstractItemModel 也是
// index(), parent(), rowCount(), columnCount(), data() 是原语
// 基类基于这些原语实现了 beginInsertRows(), endInsertRows() 等复杂方法
```

### 5.10 Visitor（访问者）

> **意图**：将**操作**与**对象结构**分离，使你可以不修改元素类而增加新的操作。

#### 生产场景：报表系统的"格式战争"

你做一个企业报表系统，有 4 种图表类型：柱状图、折线图、饼图、数据表格。

现在要导出这 4 种图表到 3 种格式：PDF、Excel、HTML。你第一反应是在每个图表类里加方法：

```cpp
class BarChart {
    void exportToPDF()  { /* 柱状图的 PDF 绘制逻辑 */ }
    void exportToExcel(){ /* 柱状图的 Excel 写入逻辑 */ }
    void exportToHTML() { /* 柱状图的 HTML 渲染逻辑 */ }
};
// PieChart、LineChart、DataTable 各写一遍...
```

4 种图表 × 3 种导出格式 = 12 个方法分散在 4 个类里。现在客户说"再加一个导出 Markdown 格式"——你得到 4 个文件里各加一个 `exportToMarkdown()`。如果之后加第 5 种图表"雷达图"，又要在 3 个导出格式中各写一遍。

**核心问题：你需要对一组固定的对象结构（各种图表类型）执行多种不同的操作（各种导出格式）。如果操作逻辑散落在各个对象类里，加一种操作就要改所有对象类。** Visitor 的思路是反过来的：把操作（导出）集中在一个类里，让它"访问"每种图表——加一个新格式只需新增一个 Visitor，不改任何图表类。

#### 结构

```mermaid
classDiagram
    class Visitor {
        +visit(NumberNode)
        +visit(PlusNode)
    }
    class TypeChecker {
        +visit(NumberNode)
        +visit(PlusNode)
    }
    class CodeGenerator {
        +visit(NumberNode)
        +visit(PlusNode)
    }
    class Element {
        +accept(Visitor)
    }
    class NumberNode {
        +accept(Visitor)
    }
    class PlusNode {
        +accept(Visitor)
    }

    Visitor <|-- TypeChecker
    Visitor <|-- CodeGenerator
    Element <|-- NumberNode
    Element <|-- PlusNode
    Element --> Visitor : accepts
```

#### C++ 实现（双重分派）

```cpp
// 前置声明
class NumberNode;
class PlusNode;

// Visitor 接口
class INodeVisitor {
public:
    virtual ~INodeVisitor() = default;
    virtual void visit(NumberNode& node) = 0;
    virtual void visit(PlusNode& node) = 0;
};

// Element 接口
class AstNode {
public:
    virtual ~AstNode() = default;
    virtual void accept(INodeVisitor& visitor) = 0;
};

// 具体元素
class NumberNode : public AstNode {
public:
    double value = 0;

    void accept(INodeVisitor& visitor) override {
        visitor.visit(*this);  // 第一次分派：调用重载的 visit(NumberNode&)
    }
};

class PlusNode : public AstNode {
public:
    std::unique_ptr<AstNode> left;
    std::unique_ptr<AstNode> right;

    void accept(INodeVisitor& visitor) override {
        visitor.visit(*this);  // 第一次分派：调用重载的 visit(PlusNode&)
    }
};

// 具体 Visitor：代码生成
class CodeGenVisitor : public INodeVisitor {
public:
    void visit(NumberNode& node) override {
        m_result = QString::number(node.value);
    }

    void visit(PlusNode& node) override {
        node.left->accept(*this);
        QString left = m_result;
        node.right->accept(*this);
        QString right = m_result;
        m_result = QString("(%1 + %2)").arg(left, right);
    }

    QString result() const { return m_result; }

private:
    QString m_result;
};

// 使用
auto ast = std::make_unique<PlusNode>();
ast->left = std::make_unique<NumberNode>(); ast->left->value = 1;
ast->right = std::make_unique<NumberNode>(); ast->right->value = 2;

CodeGenVisitor gen;
ast->accept(gen);
qDebug() << gen.result();  // "(1 + 2)"
```

> [!warning] Visitor 的陷阱
> Visitor 在 C++ 中需要*双重分派*，代码较繁琐。它适合**元素类型稳定但操作频繁变化**的场景。如果元素类型经常变（比如 AST 新增节点类型），Visitor 会导致所有 Visitor 子类都需要修改——此时考虑用 `std::variant` + `std::visit` 代替。

#### C++17 替代方案：std::variant

```cpp
// 现代 C++ 中，variant + visit 很多时候比 Visitor 模式更简洁
using AstNode = std::variant<double,  // NumberNode
                            std::tuple<std::unique_ptr<AstNode>,
                                       std::unique_ptr<AstNode>>>;  // PlusNode

struct CodeGenerator {
    QString operator()(double value) {
        return QString::number(value);
    }
    QString operator()(const std::tuple<std::unique_ptr<AstNode>,
                                        std::unique_ptr<AstNode>>& pair) {
        auto& [left, right] = pair;
        return QString("(%1 + %2)")
            .arg(std::visit(CodeGenerator{}, *left))
            .arg(std::visit(CodeGenerator{}, *right));
    }
};
// 用法：std::visit(CodeGenerator{}, node);
```

### 5.11 Interpreter（解释器）

> **意图**：为一种语言定义其文法的表示，并提供一个解释器来处理这种文法的句子。

#### 生产场景：自定义过滤器的"表达式引擎"

你做一个数据管理后台，产品提了一个需求：让用户在搜索框里输入自定义表达式，比如：

```
amount > 1000 AND (status = "paid" OR priority >= 5)
```

你需要解析这个字符串，理解 `AND`/`OR` 的优先级、比较运算符的语义，然后生成对应的 SQL WHERE 子句。这就是一个微型语言的"解释器"。

> [!note] 实际建议
> Interpreter 是 GoF 23 种模式中使用频率最低的。在 C++/Qt 中，与其手写语法树 + 递归下降解析器，通常用现成工具更实际：`QRegularExpression`（简单匹配）、ANTLR/flex+bison（完整 DSL）、或直接嵌入 Lua/Python 脚本引擎。

```cpp
// Qt 中：QRegularExpression 就是最常用的"解释器"
QRegularExpression re(R"((\d+)\s*([+\-*/])\s*(\d+))");
auto match = re.match("3 + 5");
if (match.hasMatch()) {
    int a = match.captured(1).toInt();
    QString op = match.captured(2);
    int b = match.captured(3).toInt();
    // 解释执行...
}
```

---

## 6. 模式关系与对比

### 6.1 容易混淆的模式对

| 模式 A | 模式 B | 区别 |
|-------|-------|------|
| **Factory Method** | **Abstract Factory** | FM 创建**单个**产品（一个方法），AF 创建**一族**产品（多个方法） |
| **Strategy** | **State** | Strategy 由**客户端**主动切换算法；State 由**内部状态**自动触发行为变化 |
| **Adapter** | **Decorator** | Adapter 改变**接口**，Decorator 不改变接口但**增强功能** |
| **Composite** | **Decorator** | Composite 管理**多个**子对象（一对多），Decorator 包装**一个**对象（一对一） |
| **Bridge** | **Strategy** | Bridge 是**结构型**（分离抽象与实现），Strategy 是**行为型**（运行时切换算法）。结构相似，意图不同 |
| **Template Method** | **Strategy** | TM 通过**继承**变化（编译时决定），Strategy 通过**组合**变化（运行时决定） |
| **Proxy** | **Decorator** | Proxy 控制**访问**（可能不增强功能），Decorator 增加**职责** |

### 6.2 模式组合：常见的搭配

```mermaid
flowchart TD
    subgraph QT["Qt 中常见的模式组合"]
        subgraph A["QObject 对象树"]
            A1["Composite<br>parent-child 树形结构"]
            A2["Observer<br>信号槽通信"]
        end
        subgraph B["QUndoStack / QUndoCommand"]
            B1["Command<br>封装操作"]
            B2["Memento<br>保存 undo 状态快照"]
        end
        subgraph C["QStyle / QStyleOption"]
            C1["Bridge<br>抽象控件 vs 平台绘制"]
            C2["Strategy<br>运行时切换风格"]
            C3["Abstract Factory<br>创建匹配的控件族"]
        end
        subgraph D["QAbstractItemModel + View"]
            D1["Adapter<br>适配不同数据源"]
            D2["Observer<br>Model 变化 → View 刷新"]
            D3["Composite<br>树形数据索引"]
            D4["Proxy<br>QSortFilterProxyModel"]
        end
    end
    style QT fill:#f8fafc,stroke:#e2e8f0
```

---

## 7. 关键概念速查表

| 模式 | 类别 | 一句话 | C++ 关键技法 | Qt 实例 |
|------|------|--------|-------------|--------|
| **Singleton** | 创建型 | 全局唯一实例 | 静态局部变量 + `= delete` | `QApplication` |
| **Factory Method** | 创建型 | 子类决定创建什么 | 虚函数 + 模板方法 | `QPluginLoader` |
| **Abstract Factory** | 创建型 | 创建一族相关对象 | 多虚函数工厂接口 | `QStyle` |
| **Builder** | 创建型 | 分步构建复杂对象 | 流式接口 + `build()` | `QNetworkRequest` |
| **Prototype** | 创建型 | 克隆已有对象 | 拷贝构造 / `clone()` | `QFont` 拷贝 |
| **Adapter** | 结构型 | 转换不兼容接口 | 组合 + 委托 | `QFileSystemModel` |
| **Bridge** | 结构型 | 抽象与实现解耦 | 两个独立继承体系 | `QAbstractButton` + `QStyle` |
| **Composite** | 结构型 | 树形"部分-整体" | 统一接口 + 递归 | `QObject` 对象树 |
| **Decorator** | 结构型 | 动态附加职责 | 组合 + 转发 | 流式包装 |
| **Facade** | 结构型 | 统一高层入口 | 封装子系统 | `QFileDialog` |
| **Flyweight** | 结构型 | 共享细粒度对象 | 工厂 + 对象池 | `QPixmapCache` |
| **Proxy** | 结构型 | 控制对象访问 | 同接口 + 延迟/拦截 | `QSortFilterProxyModel` |
| **Chain of Resp.** | 行为型 | 沿链传递请求 | 链表 + 后继指针 | Qt 事件传播 |
| **Command** | 行为型 | 对象化操作请求 | execute/undo 接口 | `QUndoStack` |
| **Iterator** | 行为型 | 遍历聚合元素 | begin()/end() | `QListIterator` |
| **Mediator** | 行为型 | 集中协调交互 | 中介者持有所有同事引用 | 信号槽协调控件 |
| **Memento** | 行为型 | 外部化内部状态 | `friend class` | 游戏存档 |
| **Observer** | 行为型 | 一对多状态通知 | 回调/信号列表 | Qt 信号槽 |
| **State** | 行为型 | 状态决定行为 | 状态对象 + 委托 | `QStateMachine` |
| **Strategy** | 行为型 | 算法可替换 | 策略接口 + 组合 | `QValidator` |
| **Template Method** | 行为型 | 算法骨架延迟到子类 | 非虚接口 + 虚函数 | `QThread::run()` |
| **Visitor** | 行为型 | 操作与结构分离 | 双重分派 | AST 处理 |
| **Interpreter** | 行为型 | 解释执行语言文法 | 文法类层次 | `QRegularExpression` |

## 8. 学习路线建议

```mermaid
flowchart TD
    START["<b>设计模式学习路线</b>"] --> P1["<b>阶段 1：高频模式 (80% 场景)</b>"]
    START --> P2["<b>阶段 2：深入理解</b>"]
    START --> P3["<b>阶段 3：按需学习</b>"]

    P1 --> P1A["Observer — Qt 信号槽的核心"]
    P1 --> P1B["Strategy — 消灭 if-else"]
    P1 --> P1C["Factory Method — 解耦对象创建"]
    P1 --> P1D["Singleton — 了解陷阱"]
    P1 --> P1E["Adapter — 整合第三方库"]
    P1 --> P1F["Command — 撤销/重做"]
    P1 --> P1G["Composite — 树形结构"]

    P2 --> P2A["Template Method — 框架设计"]
    P2 --> P2B["State — 告别状态标志位"]
    P2 --> P2C["Decorator — 灵活扩展"]
    P2 --> P2D["Bridge — 大型系统解耦"]
    P2 --> P2E["Proxy — 延迟加载/权限控制"]

    P3 --> P3A["Builder → 构造参数太多"]
    P3 --> P3B["Abstract Factory → 产品族"]
    P3 --> P3C["Flyweight → 大量相似对象"]
    P3 --> P3D["Chain of Resp. → 多级审批"]
    P3 --> P3E["Mediator → 组件交互复杂"]
    P3 --> P3F["Memento → 快照/存档"]
    P3 --> P3G["Visitor → AST/稳定结构"]
    P3 --> P3H["Prototype → 模板克隆"]
    P3 --> P3I["Interpreter → 自定义 DSL"]

    style P1 fill:#dbeafe,stroke:#2563eb
    style P2 fill:#ede9fe,stroke:#7c3aed
    style P3 fill:#ecfdf5,stroke:#059669
```

---

## 参考资料

- Gamma, E., Helm, R., Johnson, R., & Vlissides, J. (1994). *Design Patterns: Elements of Reusable Object-Oriented Software*. Addison-Wesley.
- Qt 源码：`QObject`, `QUndoStack`, `QStyle`, `QStateMachine`, `QSortFilterProxyModel`
- C++ Core Guidelines: [C.120](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines#c120-use-class-hierarchies-to-represent-concepts-with-inherent-hierarchical-structure-only) — 类层次结构的使用原则
